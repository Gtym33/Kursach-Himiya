#Хрупкое единство#
RSS_esser_party_struggle_decisions = {
	######################
	# Блок Максималистов #
	######################
	# Програмная статья
		# правого искажения
	rad_soc_programm_against_right = {
		icon = eng_trade_unions_support
		available = { 
			always = yes 
			NOT = { has_country_flag = rad_soc_choose_program }
			check_variable = { RSS_patry_activity_count < 1 }
			NOT = { has_country_flag = rad_soc_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			NOT = { has_country_flag = rad_soc_choose_program_start }
			NOT = { has_country_flag = rad_soc_choose_program }
		}
		cost = 10
		days_remove = 7
		complete_effect = {
			set_country_flag = rad_soc_choose_program_start
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_popularity = {
				ideology = market_liberalism
				popularity = -0.01
			}
			set_country_flag = rad_soc_choose_program
			clr_country_flag = rad_soc_choose_program_start
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	rad_soc_programm_against_right_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 7
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = -0.01
				}
				set_country_flag = rad_soc_choose_program
				rad_soc_makes_move_ai = yes
			}
		}
	}
		# умеренного искажения
	rad_soc_programm_against_moderate = {
		icon = eng_trade_unions_support
		available = { 
			always = yes 
			NOT = { has_country_flag = rad_soc_choose_program }
			check_variable = { RSS_patry_activity_count < 1 }
			NOT = { has_country_flag = rad_soc_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			NOT = { has_country_flag = rad_soc_choose_program_start }
			NOT = { has_country_flag = rad_soc_choose_program }
		}
		cost = 10
		days_remove = 7
		complete_effect = {
			set_country_flag = rad_soc_choose_program_start
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_popularity = {
				ideology = social_liberalism
				popularity = -0.01
			}
			set_country_flag = rad_soc_choose_program
			clr_country_flag = rad_soc_choose_program_start
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	rad_soc_programm_against_moderate_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 7
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = social_liberalism
					popularity = -0.01
				}
				set_country_flag = rad_soc_choose_program
				rad_soc_makes_move_ai = yes
			}
		}
	}
	# Органзовать митинги сторонников
	rad_soc_organise_rally = {
		icon = generic_political_rally
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = rad_soc_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_soc_choose_program
		}
		cost = 40
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_popularity = {
				ideology = radical_socialism
				popularity = 0.05
			}
			set_country_flag = rad_soc_organised_rally_once
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}	
	}
	rad_soc_organise_rally_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
				ideology = radical_socialism
				popularity = 0.05
			}
				set_country_flag = rad_soc_organised_rally_once
				rad_soc_makes_move_ai = yes
			}
		}
	}
	# Провести съезд сторонников 
	rad_soc_supporters_congress = {
		icon = eng_blackshirt_march
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = rad_soc_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_soc_organised_rally_once
		}
		cancel_trigger = {
			NOT = { has_country_flag = rad_soc_supporters_congress  }
		}
		cost = 70
		days_remove = 20
		cancel_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
		complete_effect = {
			set_country_flag = rad_soc_supporters_congress
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_popularity = {
				ideology = radical_socialism
				popularity = 0.05
			}
			clr_country_flag = rad_soc_supporters_congress
			set_country_flag = rad_soc_supporters_congress_once 
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	rad_soc_supporters_congress_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 20
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
				ideology = radical_socialism
				popularity = 0.05
			}
				set_country_flag = rad_soc_supporters_congress_once
				rad_soc_makes_move_ai = yes
			}
		}
	}
	# Выдвинуть вопрос о верховной власти
	rad_soc_question_of_power = {
		icon = eng_trade_unions_demand
		available = { 
			AND = {
				custom_trigger_tooltip = {
					tooltip = party_pop_most_rs
					check_variable = { party_popularity_100@radical_socialism > party_popularity_100@social_liberalism }
					check_variable = { party_popularity_100@radical_socialism > party_popularity_100@market_liberalism }
				}
				custom_trigger_tooltip = {
					tooltip = party_pop_45_tt
					check_variable = { party_popularity_100@radical_socialism > 45 }
				}
				check_variable = { RSS_patry_activity_count < 1 }
				NOT = { has_country_flag = rad_soc_banned }
			}
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_soc_supporters_congress_once
		}
		cost = 50
		days_remove = 7
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			set_country_flag = rad_cos_ultimatum
			country_event = { id = rcw_rss_esser.7 }
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	rad_soc_question_of_power_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 7
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			set_country_flag = rad_cos_ultimatum
			country_event = { id = rcw_rss_esser.7 }
		}
	}
	# Агитация на заводах 
	rad_soc_agitation_in_factories = {
		icon = eng_propaganda_campaigns
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = rad_soc_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
		}
		cost = 30
		days_remove = 20
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_popularity = {
				ideology = radical_socialism
				popularity = 0.03
			}
			set_country_flag = rad_soc_did_factories_agitation_once
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
		}
	}
	rad_soc_agitation_in_factories_ai_mission = {
		icon = eng_propaganda_campaigns
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 20
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = radical_socialism
					popularity = 0.03
				}
				set_country_flag = rad_soc_did_factories_agitation_once
				rad_soc_makes_move_ai = yes
			}
		}
	}
	# Поддержать лояльные профсоюзы
	rad_soc_support_loyal_unions = {
		icon = generic_factory
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = rad_soc_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_soc_did_factories_agitation_once
		}
		cost = 50
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_popularity = {
				ideology = radical_socialism
				popularity = 0.04
			}
			set_country_flag = rad_soc_did_support_loyal_unions_once
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	rad_soc_support_loyal_unions_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = radical_socialism
					popularity = 0.04
				}
				set_country_flag = rad_soc_did_support_loyal_unions_once
				rad_soc_makes_move_ai = yes
			}
		}
	}
	# Организовать забастовку
	rad_soc_organize_strike = {
		icon = generic_political_rally
		available = {
			AND = {
				custom_trigger_tooltip = {
					tooltip = medium_party_antagonism_tt
					check_variable = { RSS_party_antagonism > 3 }
				}
				custom_trigger_tooltip = {
					tooltip = party_not_dominant_tt
					OR = {
						check_variable = { party_popularity_100@market_liberalism > party_popularity_100@radical_socialism }
						check_variable = { party_popularity_100@social_liberalism > party_popularity_100@radical_socialism }
					}
				}
				NOT = { has_idea = strikes_1 }
				NOT = { has_idea = strikes_2 }
				NOT = { has_idea = strikes_3 }
				NOT = { has_idea = RSS_prohibition_of_strikes_idea }
				NOT = { has_country_flag = preperation_to_the_strike }
				check_variable = { RSS_patry_activity_count < 1 }
				NOT = { has_country_flag = rad_soc_banned }
			} 	
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_soc_did_support_loyal_unions_once
			NOT = { has_country_flag = rad_soc_did_strike_once }
		}
		cost = 50
		days_remove = 30
		complete_effect = {
			set_country_flag = preperation_to_the_strike
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			clr_country_flag = preperation_to_the_strike
			set_country_flag = rad_soc_did_strike_once
			add_popularity = {
				ideology = radical_socialism
				popularity = 0.07
			}
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
			add_timed_idea = {
				idea = strikes_1
				days = 21
			}
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	rad_soc_organize_strike_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				clr_country_flag = preperation_to_the_strike
				set_country_flag = rad_soc_did_strike_once
				add_popularity = {
					ideology = radical_socialism
					popularity = 0.07
				}
				add_to_variable = {
					var = RSS_party_antagonism
					value = 1
				}
				RSS_update_party_antagonism = yes
				add_timed_idea = {
					idea = strikes_1
					days = 21
				}
				rad_soc_makes_move_ai = yes
			}
		}
	}
	#Агитация среди крестьян
	rad_soc_agitation_peasants = {
		icon = eng_propaganda_campaigns
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = rad_soc_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
		}
		cost = 20
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_popularity = {
				ideology = radical_socialism
				popularity = 0.03
			}
			set_country_flag = rad_soc_did_peasant_agitation_once
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	rad_soc_agitation_peasants_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = radical_socialism
					popularity = 0.03
				}
				set_country_flag = rad_soc_did_peasant_agitation_once
				rad_soc_makes_move_ai = yes
			}
		}
	}
	#Продвижение сторонников в союзы трудового крестьянства
	rad_soc_support_peasant_unions = {
		icon = hol_inundate_water_lines
		available = { 
			has_completed_focus = RSS_unions_of_the_labor_peasantry
			check_variable = { RSS_patry_activity_count < 1 }
			NOT = { has_country_flag = rad_soc_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_soc_did_peasant_agitation_once
		}
		cost = 50
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_popularity = {
				ideology = radical_socialism
				popularity = 0.06
			}
			set_country_flag = rad_soc_support_peasant_unions_once
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	rad_soc_support_peasant_unions_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = radical_socialism
					popularity = 0.06
				}
				set_country_flag = rad_soc_support_peasant_unions_once
				rad_soc_makes_move_ai = yes
			}
		}
	}
	#Шантажировать город поставками
	rad_soc_blackmail_the_city_with_supplies = {
		icon = hol_attract_foreign_investors
		available = {
			AND = {
				custom_trigger_tooltip = {
					tooltip = medium_plus_party_antagonism_tt
					check_variable = { RSS_party_antagonism > 4 }
				}
				check_variable = { RSS_patry_activity_count < 1 }
				custom_trigger_tooltip = {
					tooltip = party_pop_20_not_dom_tt
					AND = {
						check_variable = { party_popularity_100@market_liberalism > party_popularity_100@radical_socialism }
						check_variable = { party_popularity_100@radical_socialism > 20 }
					}
				}
				NOT = { has_country_flag = rad_soc_banned }
			} 	
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_soc_support_peasant_unions_once
			NOT = { has_country_flag = rad_soc_did_blackmail_once }
		}
		cost = 50
		days_remove = 14
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
			set_country_flag = rad_soc_did_blackmail
			set_country_flag = rad_soc_did_blackmail_once
			hidden_effect = {
				random_list = {
					40 = { set_country_flag = bread_good_answer }
					30 = { set_country_flag = bread_middle_answer }
					30 = { set_country_flag = bread_bad_answer }
				}
			}
			country_event = { id = rcw_rss_esser.8 }
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	rad_soc_blackmail_the_city_with_supplies_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 14
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_to_variable = {
					var = RSS_party_antagonism
					value = 1
				}
				RSS_update_party_antagonism = yes
				set_country_flag = rad_soc_did_blackmail
				set_country_flag = rad_soc_did_blackmail_once
				if = {
					limit = { has_country_flag = RSS_esser_choose_soc_lib_side }
					country_event = { id = rcw_rss_esser.9 }
				}
				else_if = {
					limit = { NOT = { has_country_flag = RSS_esser_choose_soc_lib_side } }
					random_list = {
						40 = {
							add_popularity = {
								ideology = radical_socialism
								popularity = 0.07
							} 
						}
						30 = { 
							add_popularity = {
								ideology = radical_socialism
								popularity = 0.04
							}
							random_list = {
								60 = {}
								40 = { rise_foodstuffs_supply_crisis = yes }
							}
						}
						30 = {  
							add_stability = -0.05
							rise_foodstuffs_supply_crisis = yes
						}
					}
				}
				rad_soc_makes_move_ai = yes
			}
		}
	}
	#Агитация в армии
	rad_soc_agitation_in_army = {
		icon = eng_propaganda_campaigns
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = rad_soc_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
		}
		cost = 30
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			if = {
				limit = { has_country_flag = rad_soc_failed_conversation }
				add_days_remove  = {
					decision = rad_soc_agitation_in_army
					days = 20
				}
			}
			if = {
				limit = { has_country_flag = politics_in_army_banned }
				add_days_remove  = {
					decision = rad_soc_agitation_in_army
					days = 15
				}
			}
		}
		remove_effect = {
			add_war_support = -0.03
			add_popularity = {
				ideology = radical_socialism
				popularity = 0.03
			}
			set_country_flag = rad_soc_did_army_agitation_once
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
		}
	}
	rad_soc_agitation_in_army_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_war_support = -0.03
				add_popularity = {
					ideology = radical_socialism
					popularity = 0.03
				}
				set_country_flag = rad_soc_did_army_agitation_once
				rad_soc_makes_move_ai = yes
			}
		}
	}
	#Разослать лояльных комиссаров
	rad_cos_send_loyal_coms = {
		icon = generic_speech
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = rad_soc_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_soc_did_army_agitation_once
		}
		fire_only_once = yes
		cost = 70
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			if = {
				limit = { has_country_flag = rad_soc_failed_conversation }
				add_days_remove  = {
					decision = rad_cos_send_loyal_coms
					days = 20
				}
			}
			if = {
				limit = { has_country_flag = politics_in_army_banned }
				add_days_remove  = {
					decision = rad_cos_send_loyal_coms
					days = 15
				}
			}			
		}
		remove_effect = {
			set_country_flag = rad_soc_did_send_coms
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			add_popularity = {
				ideology = radical_socialism
				popularity = 0.03
			}
		}
	}
	rad_cos_send_loyal_coms_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = { 
			hidden_effect = {
				add_popularity = {
					ideology = radical_socialism
					popularity = 0.03
				}
				set_country_flag = rad_soc_did_send_coms
				rad_soc_makes_move_ai = yes
			}
		}
	}
	#Создать подпольную сеть
	rad_soc_create_secret_web = {
		icon = generic_speech
		fire_only_once = yes
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = rad_soc_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_soc_did_army_agitation_once
		}
		fire_only_once = yes
		cost = 70
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			if = {
				limit = { has_country_flag = rad_soc_failed_conversation }
				add_days_remove  = {
					decision = rad_soc_create_secret_web
					days = 20
				}
			}	
		}
		remove_effect = {
			set_country_flag = rad_soc_created_secret_web
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
		}
	}
	rad_soc_create_secret_web_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = rad_soc_created_secret_web
				rad_soc_makes_move_ai = yes
			}
		}
	}
	#Инспирировать устранение противников
		#Умереных
	rad_soc_start_killing_moderate = {
		icon = generic_assassination
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			custom_trigger_tooltip = {
				tooltip = high_party_antagonism_tt
				check_variable = { RSS_party_antagonism > 6 }
			}
			NOT = { has_idea = moderate_faction_assassinations }
			NOT = { has_country_flag = rad_soc_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_soc_created_secret_web
		}
		cost = 50
		days_remove = 28
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			if = {
				limit = { has_country_flag = rad_soc_failed_conversation }
				add_days_remove  = {
					decision = rad_soc_start_killing_moderate
					days = 20
				}
			}	
		}
		remove_effect = {
			add_ideas = moderate_faction_assassinations 
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
		}
	}
	rad_soc_start_killing_moderate_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 28
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_ideas = moderate_faction_assassinations 
				add_to_variable = {
					var = RSS_party_antagonism
					value = 1
				}
				RSS_update_party_antagonism = yes
				rad_soc_makes_move_ai = yes
			}
		}
	}
		#Правых
	rad_soc_start_killing_right = {
		icon = generic_assassination
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			custom_trigger_tooltip = {
				tooltip = high_party_antagonism_tt
				check_variable = { RSS_party_antagonism > 6 }
			}
			NOT = { has_idea = right_faction_assassinations }
			NOT = { has_country_flag = rad_soc_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_soc_created_secret_web
		}
		cost = 50
		days_remove = 28
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			if = {
				limit = { has_country_flag = rad_soc_failed_conversation }
				add_days_remove  = {
					decision = rad_soc_start_killing_right
					days = 20
				}
			}
		}
		remove_effect = {
			add_ideas = right_faction_assassinations 
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
		}
	}
	rad_soc_start_killing_right_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 28
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_ideas = right_faction_assassinations  
				add_to_variable = {
					var = RSS_party_antagonism
					value = 1
				}
				RSS_update_party_antagonism = yes
				rad_soc_makes_move_ai = yes
			}
		}
	}
	#Подготовить склады с оружием
	rad_cos_prepare_warehouse = {
		icon = generic_assassination
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			custom_trigger_tooltip = {
				tooltip = high_minus_party_antagonism_tt
				check_variable = { RSS_party_antagonism > 5 }
			}
			has_equipment = {
				infantry_equipment > 399
			}
			NOT = { has_country_flag = rad_soc_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_soc_created_secret_web
			has_country_flag = rad_soc_did_send_coms
		}
		cost = 80
		days_remove = 35
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			if = {
				limit = { has_country_flag = rad_soc_failed_conversation }
				add_days_remove  = {
					decision = rad_cos_prepare_warehouse
					days = 20
				}
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -400
			}
			set_country_flag = rad_cos_prepared_warehouse
		}
	}
	rad_cos_prepare_warehouse_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 35
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = rad_cos_prepared_warehouse
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = -400
				}
				rad_soc_makes_move_ai = yes
			}
		}
	}
	#Вооруженная демонстрация 
	rad_cos_arrest_gov = {
		icon = generic_assassination
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			custom_trigger_tooltip = {
				tooltip = high_plus_party_antagonism_tt
				check_variable = { RSS_party_antagonism > 7 }
			}
			has_stability < 0.4 
			NOT = { has_country_flag = rad_soc_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_cos_prepared_warehouse
			has_country_flag = rad_soc_did_send_coms
		}
		cost = 50
		days_remove = 21
		cancel_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } }
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}	
			if = {
				limit = { has_country_flag = rad_soc_failed_conversation }
				add_days_remove  = {
					decision = rad_cos_arrest_gov
					days = 20
				}
			}
			activate_mission = sus_rad_soc_rally_mission
			set_country_flag = rad_soc_arrest_gov_start
		}
		remove_effect = {
			set_country_flag = rad_soc_arrest_gov
			clr_country_flag = rad_soc_arrest_gov_start
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			country_event = { id = rcw_rss_esser.23 }			
		}
	}
	rad_cos_arrest_gov_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 21
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = rad_soc_arrest_gov
				clr_country_flag = rad_soc_arrest_gov_start
				country_event = { id = rcw_rss_esser.23 }
			}
		}
	}
	#Вербовка местных коммунистов
	rad_cos_recruit_communists = {
		icon = generic_nationalism
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			NOT = { has_country_flag = rad_soc_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
		}
		fire_only_once = yes
		cost = 30
		days_remove = 14
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			add_popularity = {
				ideology = communism
				popularity = 0.03
			}
			add_popularity = {
				ideology = radical_socialism
				popularity = 0.03
			}
			add_stability = -0.04
		}
	}
	rad_cos_recruit_communists_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 14
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = did_rad_cos_recruit_communists_ai_mission
				add_popularity = {
					ideology = communism
					popularity = 0.03
				}
				add_popularity = {
					ideology = radical_socialism
					popularity = 0.03
				}
				add_stability = -0.04
				rad_soc_makes_move_ai = yes
			}
		}
	}
	#Обратится к местным анархистам
	rad_soc_recruit_anarcists = {
		icon = generic_nationalism
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			NOT = { has_country_flag = rad_soc_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
		}
		fire_only_once = yes
		cost = 30
		days_remove = 14
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			add_popularity = {
				ideology = radical_socialism
				popularity = 0.04
			}
			add_stability = -0.04
		}
	}
	rad_soc_recruit_anarcists_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 14
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = did_rad_soc_recruit_anarcists_ai_mission
				add_popularity = {
					ideology = radical_socialism
					popularity = 0.04
				}
				add_stability = -0.04
				rad_soc_makes_move_ai = yes
			}
		}
	}
	#Беседа с новым главкомом
	rad_soc_conversation_with_army_chief = {
		icon = generic_army_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			NOT = { has_country_flag = army_chief_conversation_happened }
			NOT = { has_country_flag = rad_soc_banned }
			OR = {
				has_idea = Radola_Gaida_army_chief
				has_idea = Fyodor_Makhin_army_chief
				has_idea = Vasiliy_Boldyrev_army_chief
			}
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			NOT = { has_country_flag = army_chief_conversation_happened }
		}
		cost = 5
		days_remove = 5
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		fire_only_once = yes
		complete_effect = {
			set_country_flag = army_chief_conversation_happened
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			set_country_flag = rad_soc_did_conversation
			country_event = { id = rcw_rss_esser.10 }
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
		}
	}
	rad_soc_conversation_with_army_chief_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 5
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = rad_soc_did_conversation
				if = {
					limit = { has_idea = Fyodor_Makhin_army_chief }
					random_list = {
						25 = { set_country_flag = rad_soc_army_chief_recruted }
						75 = { 
							random_list = {
								50 = { 
									random_list = {
										60 = { country_event = { id = rcw_rss_esser.13 } }
										40 = { country_event = { id = rcw_rss_esser.14 } }
									}
								}
								50 = { country_event = { id = rcw_rss_esser.14 } }
							}
						}
					}
				}
				else_if = {
					limit = { has_idea = Radola_Gaida_army_chief }
					random_list = {
						70 = { set_country_flag = rad_soc_army_chief_recruted }
						30 = { 
							random_list = {
								50 = { 
									random_list = {
										60 = { country_event = { id = rcw_rss_esser.13 } }
										40 = { country_event = { id = rcw_rss_esser.14 } }
									}
								}
								50 = { country_event = { id = rcw_rss_esser.14 } }
							} 
						}
					}
				}
				else_if = {
					limit = { has_idea = Vasiliy_Boldyrev_army_chief }
					random_list = {
						10 = {
							set_country_flag = rad_soc_army_chief_recruted
							add_popularity = {
								ideology = national_syndicalism
								popularity = 0.03
							}
						}
						90 = { 
							random_list = {
								50 = { 
									random_list = {
										60 = { country_event = { id = rcw_rss_esser.13 } }
										40 = { country_event = { id = rcw_rss_esser.14 } }
									}
								}
								50 = { country_event = { id = rcw_rss_esser.14 } }
							}
						}
					}
				}
				rad_soc_makes_move_ai = yes
			}
		}
	}
	#Выпустить «Грамоту» максималистов 
	release_maximalist_paper = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			
		}
		visible = { 
			has_country_flag = RSS_esser_choose_rad_soc_side
			has_country_flag = rad_soc_did_army_agitation_once
		}
		cost = 25
		days_remove = 14
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
			country_event = { id = rcw_rss_esser.54 }
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			set_country_flag = released_maximalist_paper
		}
	}
	release_maximalist_paper_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 14
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_to_variable = {
					var = RSS_party_antagonism
					value = 1
				}
				RSS_update_party_antagonism = yes
				country_event = { id = rcw_rss_esser.54 }
				rad_soc_makes_move_ai = yes
			}
		}
	}
	##################
	# Блок Умеренных #
	##################
	#Пойти на переговоры с максималистами 
	moderate_negotiation_with_rad_soc = {
		icon = generic_political_discourse
		available = {  
			check_variable = { RSS_patry_activity_count < 1 }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			NOT = { has_country_flag = rad_soc_banned }
		}
		cost = 40
		days_remove = 35
		modifier = {
			social_liberalism_drift = 0.05
		}
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			hidden_effect = {
				add_days_mission_timeout = {
					mission = rad_soc_programm_against_right_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_soc_programm_against_moderate_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_soc_organise_rally_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_soc_supporters_congress_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_soc_question_of_power_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_soc_agitation_in_factories_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_soc_support_loyal_unions_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_soc_agitation_peasants_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_soc_support_peasant_unions_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_soc_blackmail_the_city_with_supplies_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_soc_agitation_in_army_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_cos_send_loyal_coms_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_soc_create_secret_web_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_cos_recruit_communists_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_soc_recruit_anarcists_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = rad_soc_conversation_with_army_chief_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = release_maximalist_paper_ai_mission
					days = 15
				}
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	moderate_negotiation_with_rad_soc_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 35
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				if = {
					limit = { has_country_flag = RSS_esser_choose_rad_soc_side }
					add_days_remove = {
						decision  = rad_soc_programm_against_right
						days = 15
					}
					add_days_remove = {
						decision  = rad_soc_programm_against_moderate
						days = 15
					}
					add_days_remove = {
						decision  = rad_soc_organise_rally
						days = 15
					}
					add_days_remove = {
						decision  = rad_soc_supporters_congress
						days = 15
					}
					add_days_remove = {
						decision  = rad_soc_question_of_power
						days = 15
					}
					add_days_remove = {
						decision  = rad_soc_agitation_in_factories
						days = 15
					}
					add_days_remove = {
						decision  = rad_soc_support_loyal_unions
						days = 15
					}
					add_days_remove = {
						decision  = rad_soc_agitation_peasants
						days = 15
					}
					add_days_remove = {
						decision  = rad_soc_support_peasant_unions
						days = 15
					}
					add_days_remove = {
						decision  = rad_soc_blackmail_the_city_with_supplies
						days = 15
					}
					add_days_remove = {
						decision  = rad_soc_agitation_in_army
						days = 15
					}
					add_days_remove = {
						decision  = rad_cos_send_loyal_coms
						days = 15
					}
					add_days_remove = {
						decision  = rad_soc_create_secret_web
						days = 15
					}
					add_days_remove = {
						decision  = rad_cos_recruit_communists
						days = 15
					}
					add_days_remove = {
						decision  = rad_soc_recruit_anarcists
						days = 15
					}
					add_days_remove = {
						decision  = rad_soc_conversation_with_army_chief
						days = 15
					}
					add_days_remove = {
						decision  = release_maximalist_paper
						days = 15
					}
				}
				else_if = {
					limit = { has_country_flag = RSS_esser_choose_market_lib_side }
					add_days_mission_timeout = {
						mission = rad_soc_programm_against_right_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_soc_programm_against_moderate_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_soc_organise_rally_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_soc_supporters_congress_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_soc_question_of_power_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_soc_agitation_in_factories_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_soc_support_loyal_unions_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_soc_agitation_peasants_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_soc_support_peasant_unions_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_soc_blackmail_the_city_with_supplies_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_soc_agitation_in_army_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_cos_send_loyal_coms_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_soc_create_secret_web_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_cos_recruit_communists_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_soc_recruit_anarcists_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = rad_soc_conversation_with_army_chief_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = release_maximalist_paper_ai_mission
						days = 15
					}
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	#Пойти на переговоры с правыми 
	moderate_negotiation_with_market_lib = {
		icon = generic_political_discourse
		available = {  
			check_variable = { RSS_patry_activity_count < 1 }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			NOT = { has_country_flag = market_lib_banned }
		}
		cost = 40
		days_remove = 35
		modifier = {
			social_liberalism_drift = 0.05
		}
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			hidden_effect = {
				add_days_mission_timeout = {
					mission = market_lib_programm_against_maximalist_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_programm_against_moderate_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_organise_rally_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_supporters_congress_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_question_of_power_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_agitation_in_factories_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_support_loyal_unions_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_agitation_peasants_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_support_peasant_unions_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_blackmail_the_city_with_supplies_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_agitation_in_army_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_send_loyal_coms_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_create_secret_web_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_conversation_with_army_chief_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_coalition_with_cadets_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_cadet_ties_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_cadet_press_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_cadet_rally_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_cadet_unite_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = market_lib_nominal_ai_mission
					days = 15
				}
				add_days_mission_timeout = {
					mission = declare_maximalist_german_agents_ai_mission
					days = 15
				}
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	moderate_negotiation_with_market_lib_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 35
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				if = {
					limit = { has_country_flag = RSS_esser_choose_market_lib_side }
					add_days_remove = {
						decision  = market_lib_programm_against_maximalist
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_programm_against_moderate
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_organise_rally
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_supporters_congress
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_question_of_power
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_agitation_in_factories
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_support_loyal_unions
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_agitation_peasants
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_support_peasant_unions
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_blackmail_the_city_with_supplies
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_agitation_in_army
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_send_loyal_coms
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_create_secret_web
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_coalition_with_cadets
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_cadet_ties
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_cadet_press
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_cadet_rally
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_cadet_unite
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_nominal
						days = 15
					}
					add_days_remove = {
						decision  = market_lib_conversation_with_army_chief
						days = 15
					}
					add_days_remove = {
						decision  = declare_maximalist_german_agents
						days = 15
					}
				}
				else_if = {
					limit = { has_country_flag = RSS_esser_choose_rad_soc_side }
					add_days_mission_timeout = {
						mission = market_lib_programm_against_maximalist_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_programm_against_moderate_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_organise_rally_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_supporters_congress_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_question_of_power_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_agitation_in_factories_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_support_loyal_unions_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_agitation_peasants_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_support_peasant_unions_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_blackmail_the_city_with_supplies_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_agitation_in_army_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_send_loyal_coms_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_create_secret_web_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_conversation_with_army_chief_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_coalition_with_cadets_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_cadet_ties_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_cadet_press_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_cadet_rally_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_cadet_unite_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = market_lib_nominal_ai_mission
						days = 15
					}
					add_days_mission_timeout = {
						mission = declare_maximalist_german_agents_ai_mission
						days = 15
					}
				}
				
				moderate_makes_move_ai = yes
			}
		}
	}
	#Запрет политики в армии
	moderate_ban_army_politics = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
		}
		cost = 90
		days_remove = 5
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			set_country_flag = politics_in_army_banned
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			add_popularity = {
				ideology = national_syndicalism
				popularity = 0.03
			}
			hidden_effect = {
				if = {
					limit = { has_idea = Fyodor_Makhin_army_chief }
					country_event = { id = rcw_rss_esser.16 }
				}
			}
		}
	}
	moderate_ban_army_politics_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 5
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = politics_in_army_banned
				add_popularity = {
					ideology = national_syndicalism
					popularity = 0.03
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	#Расследование деятельности максималистов в армии
	moderate_investigate_rad_soc_in_army = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = politics_in_army_banned
			NOT = { has_country_flag = rad_soc_banned }
		}
		cost = 40
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			hidden_effect = {
				if = {
					limit = {
						OR = {
							has_country_flag = rad_soc_created_secret_web
							has_country_flag = rad_cos_prepared_warehouse
						}
					}
					random_list = {
						80 = { 
							country_event = { id = rcw_rss_esser.17 }  
						}
						20 = {}
					}
				}
			}
		}
	}
	moderate_investigate_rad_soc_in_army_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				if = {
					limit = {
						OR = {
							has_country_flag = rad_soc_created_secret_web
							has_country_flag = rad_cos_prepared_warehouse
						}
					}
					random_list = {
						80 = { 
							random_list = {
								33 = { set_country_flag = rs_res_1 }
								33 = { set_country_flag = rs_res_2 }
								33 = { set_country_flag = rs_res_3 } 
							}
							country_event = { id = rcw_rss_esser.40 }  
						}
						20 = {}
					}
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	#Расследование деятельности правых в армии 
	moderate_investigate_market_lib_in_army = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = politics_in_army_banned
			NOT = { has_country_flag = market_lib_banned }
		}
		cost = 40
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			hidden_effect = {
				if = {
					limit = {
						OR = {
							has_country_flag = market_lib_created_secret_web
							has_country_flag = market_lib_prepared_warehouse
						}
					}
					random_list = {
						80 = { 
							country_event = { id = rcw_rss_esser.22 }  
						}
						20 = {}
					}
				}
			}
		}
	}
	moderate_investigate_market_lib_in_army_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				if = {
					limit = {
						OR = {
							has_country_flag = market_lib_created_secret_web
							has_country_flag = market_lib_prepared_warehouse
						}
					}
					random_list = {
						80 = { 
							random_list = {
								33 = { set_country_flag = rs_res_1 }
								33 = { set_country_flag = rs_res_2 }
								33 = { set_country_flag = rs_res_3 } 
							}
							country_event = { id = rcw_rss_esser.40 }  
						}
						20 = {}
					}
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	#Запретить съезд максималистов
	moderate_ban_rad_soc_congress = {
		icon = eng_trade_unions_support
		available = { 
			custom_trigger_tooltip = {
				tooltip = party_pop_5_tt
				check_variable = { party_popularity_100@social_liberalism > 5 }
			}
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_active_mission = rad_soc_supporters_congress_ai_mission
		}
		cost = 25
		complete_effect = {
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
			remove_mission = rad_soc_supporters_congress_ai_mission
			rad_soc_makes_move_ai = yes
		}
	}
	#Запретить съезд правых
	moderate_ban_market_lib_congress = {
		icon = eng_trade_unions_support
		available = { 
			custom_trigger_tooltip = {
				tooltip = party_pop_5_tt
				check_variable = { party_popularity_100@social_liberalism > 5 }
			}
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_active_mission = market_lib_supporters_congress_ai_mission
		}
		cost = 25
		complete_effect = {
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
			remove_mission = market_lib_supporters_congress_ai_mission
			market_lib_makes_move_ai = yes
		}
	}
	#Подозрительная демонстрация Максималистов не ИИ 
	sus_rad_soc_rally_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 7
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = rad_soc_arrest_gov } }
		timeout_effect = {
			hidden_effect = {
				random_list = {
					25 = {set_country_flag = RSS_result_1}
					25 = {set_country_flag = RSS_result_2}
					50 = {}
				}
				country_event = { id = rcw_rss_esser.24 }
			}
		}
	}
	#Подозрительная демонстрация Правых не ИИ
	sus_market_lib_rally_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 7
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } has_country_flag = market_lib_arrest_gov } }
		timeout_effect = {
			hidden_effect = {
				random_list = {
					25 = {set_country_flag = RSS_result_1}
					25 = {set_country_flag = RSS_result_2}
					50 = {}
				}
				country_event = { id = rcw_rss_esser.25 }
			}
		}
	}
	#Арест вооруженной оппозиции Максималисты
	arest_rad_soc_terrorist = {
		icon = eng_trade_unions_support
		available = { 
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = can_arest_rad_soc_opposition
		}
		cost = 50
		days_remove = 4
		cancel_trigger = { has_country_flag = rad_soc_arrest_gov }
		complete_effect = {}
		remove_effect = {
			country_event = { id = rcw_rss_esser.26 }
		}
	}
	#Арест вооруженной оппозиции Правые
	arest_market_lib_terrorist = {
		icon = eng_trade_unions_support
		available = { 
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = can_arest_market_lib_opposition
		}
		cost = 50
		days_remove = 4
		cancel_trigger = { has_country_flag = market_lib_arrest_gov }
		complete_effect = {}
		remove_effect = {
			country_event = { id = rcw_rss_esser.27 }
		}
	}
	#Приказать милиции восстановить порядок 
	militia_restores_order = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
			has_stability < 0.35
			custom_trigger_tooltip = {
				tooltip = high_party_antagonism_tt
				check_variable = { RSS_party_antagonism > 6 }
			}
			custom_trigger_tooltip = {
				tooltip = police_power_high
				check_variable = { RSS_police_power > 5 }
			}
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
		}
		cost = 10
		days_remove = 14
		cancel_trigger = { 
			OR = {
				has_country_flag = market_lib_arrest_gov 
				has_country_flag = rad_soc_arrest_gov
				has_country_flag = RSS_army_coup
			}
			
		}
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			custom_effect_tooltip = RSS_order_militia
			hidden_effect = {
				if = {
					limit = { check_variable = { RSS_party_antagonism < 10 } }
					country_event = { id = rcw_rss_eser_rep.21 }
				}
				else_if = {
					limit = { 
						check_variable = { RSS_party_antagonism > 8 } 
						check_variable = { party_popularity_100@social_liberalism < 25 }
					}
					country_event = { id = rcw_rss_eser_rep.22 }
				}
			}
			
			
		}
	}
	militia_restores_order_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 14
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				if = {
					limit = { check_variable = { RSS_party_antagonism < 10 } }
					country_event = { id = rcw_rss_eser_rep.21 }
				}
				else_if = {
					limit = { 
						check_variable = { RSS_party_antagonism > 8 } 
						check_variable = { party_popularity_100@social_liberalism < 25 }
					}
					country_event = { id = rcw_rss_eser_rep.22 }
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	#Привлечь бывших максималистов к работе в партии 
	recruit_banned_maximalists_to_party = {
		icon = eng_trade_unions_support
		available = { 
			has_country_flag = rad_soc_banned
			radical_socialism > 0.03
			check_variable = { RSS_patry_activity_count < 1 }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = rad_soc_banned
		}
		cost = 40
		days_remove = 35
		cancel_trigger = { 
			OR = {
				has_country_flag = market_lib_arrest_gov 
				has_country_flag = rad_soc_arrest_gov
				has_country_flag = RSS_army_coup
			}
			NOT = { has_country_flag = RSS_esser_choose_soc_lib_side }
		}
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			add_popularity = {
				ideology = radical_socialism
				popularity = -0.03
			}
			add_popularity = {
				ideology = social_liberalism
				popularity = 0.03
			}
		}
	}
	recruit_banned_maximalists_to_party_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 35
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
				ideology = radical_socialism
				popularity = -0.03
			}
			add_popularity = {
				ideology = social_liberalism
				popularity = 0.03
			}
				moderate_makes_move_ai = yes
			}
		}
	}
	#Привлечь бывших максималистов к работе в партии 
	recruit_banned_right_to_party = {
		icon = eng_trade_unions_support
		available = { 
			has_country_flag = market_lib_banned
			market_liberalism > 0.03
			check_variable = { RSS_patry_activity_count < 1 }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = market_lib_banned
		}
		cost = 40
		days_remove = 35
		cancel_trigger = { 
			OR = {
				has_country_flag = market_lib_arrest_gov 
				has_country_flag = rad_soc_arrest_gov
				has_country_flag = RSS_army_coup
			}
			NOT = { has_country_flag = RSS_esser_choose_soc_lib_side }
		}
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			add_popularity = {
				ideology = market_liberalism
				popularity = -0.03
			}
			add_popularity = {
				ideology = social_liberalism
				popularity = 0.03
			}
		}
	}
	recruit_banned_right_to_party_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 35
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = -0.03
				}
				add_popularity = {
					ideology = social_liberalism
					popularity = 0.03
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	#Удалить из власти представителей запрещенных маскималистов 
	dismiss_banned_maximalists = {
		icon = eng_trade_unions_support
		available = { 
			has_country_flag = rad_soc_banned
			radical_socialism > 0.03
			check_variable = { RSS_patry_activity_count < 1 }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = rad_soc_banned
		}
		cost = 20
		days_remove = 28
		cancel_trigger = { 
			OR = {
				has_country_flag = market_lib_arrest_gov 
				has_country_flag = rad_soc_arrest_gov
				has_country_flag = RSS_army_coup
			}
			NOT = { has_country_flag = RSS_esser_choose_soc_lib_side }
		}
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			add_popularity = {
				ideology = radical_socialism
				popularity = -0.03
			}
			add_stability = -0.03
		}
	}
	dismiss_banned_maximalists_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 28
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = radical_socialism
					popularity = -0.03
				}
				add_stability = -0.03
				moderate_makes_move_ai = yes
			}
		}
	}
	#Удалить из власти представителей запрещенных правых 
	dismiss_banned_right = {
		icon = eng_trade_unions_support
		available = { 
			has_country_flag = market_lib_banned
			market_liberalism > 0.03
			check_variable = { RSS_patry_activity_count < 1 }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = market_lib_banned
		}
		cost = 20
		days_remove = 28
		cancel_trigger = { 
			OR = {
				has_country_flag = market_lib_arrest_gov 
				has_country_flag = rad_soc_arrest_gov
				has_country_flag = RSS_army_coup
			}
			NOT = { has_country_flag = RSS_esser_choose_soc_lib_side }
		}
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			add_popularity = {
				ideology = market_liberalism
				popularity = -0.03
			}
			add_stability = -0.03
		}
	}
	dismiss_banned_right_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 28
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = -0.03
				}
				add_stability = -0.03
				moderate_makes_move_ai = yes
			}
		}
	}
	#Провести кампанию в прессе против нераскаявшихся максималистов
	campain_against_banned_maximalists = {
		icon = eng_trade_unions_support
		available = { 
			has_country_flag = rad_soc_banned
			radical_socialism > 0.03
			check_variable = { RSS_patry_activity_count < 1 }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = rad_soc_banned
		}
		cost = 30
		days_remove = 14
		cancel_trigger = { 
			OR = {
				has_country_flag = market_lib_arrest_gov 
				has_country_flag = rad_soc_arrest_gov
				has_country_flag = RSS_army_coup
			}
			NOT = { has_country_flag = RSS_esser_choose_soc_lib_side }
		}
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			add_popularity = {
				ideology = radical_socialism
				popularity = -0.03
			}
		}
	}
	campain_against_banned_maximalists_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 28
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = radical_socialism
					popularity = -0.03
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	#Провести кампанию в прессе против нераскаявшихся максималистов
	campain_against_banned_right = {
		icon = eng_trade_unions_support
		available = { 
			has_country_flag = market_lib_banned
			market_liberalism > 0.03
			check_variable = { RSS_patry_activity_count < 1 }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = market_lib_banned
		}
		cost = 30
		days_remove = 14
		cancel_trigger = { 
			OR = {
				has_country_flag = market_lib_arrest_gov 
				has_country_flag = rad_soc_arrest_gov
				has_country_flag = RSS_army_coup
			}
			NOT = { has_country_flag = RSS_esser_choose_soc_lib_side }
		}
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			add_popularity = {
				ideology = market_liberalism
				popularity = -0.03
			}
		}
	}
	campain_against_banned_right_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 28
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = -0.03
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	#Изымать для цензуры номера «Народа» 
	censor_maximalist_paper = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_completed_focus = RSS_organization_of_state_protection
		}
		cost = 15
		days_remove = 14
		cancel_trigger = { 
			hidden_trigger = {
				OR = {
					has_country_flag = market_lib_arrest_gov 
					has_country_flag = rad_soc_arrest_gov
					has_country_flag = RSS_army_coup
				}
				NOT = { has_country_flag = RSS_esser_choose_soc_lib_side }
			}
		}
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
			country_event = { id = rcw_rss_esser.55 }
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
		}
	}
	censor_maximalist_paper_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 14
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_to_variable = {
					var = RSS_party_antagonism
					value = 1
				}
				RSS_update_party_antagonism = yes
				country_event = { id = rcw_rss_esser.55 }
				moderate_makes_move_ai = yes
			}
		}
	}
	# Органзовать митинги сторонников
	moderate_organise_rally = {
		icon = generic_political_rally
		available = { check_variable = { RSS_patry_activity_count < 1 }  }
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
		}
		cost = 60
		days_remove = 45
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_popularity = {
				ideology = social_liberalism
				popularity = 0.05
			}
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}	
	}
	moderate_organise_rally_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = social_liberalism
					popularity = 0.05
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	# Усилить антибольшевситскую пропаганду
	moderate_antibolshevik_propaganda = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
			has_completed_focus = RSS_inform_bureau
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			NOT = { has_country_flag = antibolshevik_propaganda }
		}
		cost = 20
		days_remove = 20
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			set_country_flag = antibolshevik_propaganda
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			add_war_support = 0.02
		}
	}
	moderate_antibolshevik_propaganda_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 20
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_war_support = 0.02
				moderate_makes_move_ai = yes
			}
		}
	}
	# Усилить пропаганду против "белой" реакции
	moderate_antiwhite_propaganda = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
			has_completed_focus = RSS_inform_bureau
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			NOT = { has_country_flag = antiwhite_propaganda }
		}
		cost = 20
		days_remove = 20
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			set_country_flag = antiwhite_propaganda
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			add_war_support = 0.02
			add_popularity = {
				ideology = authoritarism
				popularity = -0.04
			}
		}
	}
	moderate_antiwhite_propaganda_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 20
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = antiwhite_propaganda
				add_war_support = 0.02
				moderate_makes_move_ai = yes
			}
		}
	}
	# Разоблачить союз большевиков и черносотенцев 
	moderate_uncover_black_red_union = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
			
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = antiwhite_propaganda
			has_country_flag = antibolshevik_propaganda
		}
		cost = 20
		days_remove = 20
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			set_country_flag = did_uncover_black_red_union
			add_popularity = {
				ideology = social_liberalism
				popularity = 0.02
			}
		}
	}
	moderate_uncover_black_red_union_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 20
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = social_liberalism
					popularity = 0.02
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	# Обсудить "статьи" платформ на Съезде 
	moderate_discuss_fraction_articales = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
			
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = rad_soc_choose_program
			has_country_flag = market_lib_choose_program
		}
		cost = 10
		days_remove = 30
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			set_country_flag = did_discuss_fraction_articales
			add_stability = 0.01
		}
	}
	moderate_discuss_fraction_articales_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_stability = 0.01
				moderate_makes_move_ai = yes
			}
		}
	}
	# Провести дискуссию по аграрной реформе
	moderate_discuss_agrarian_reform = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
			
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = did_discuss_fraction_articales
		}
		cost = 10
		days_remove = 30
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			set_country_flag = did_discuss_agrarian_reform
			add_popularity = {
				ideology = social_liberalism
				popularity = 0.01
			}
		}
	}
	moderate_discuss_agrarian_reform_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = social_liberalism
					popularity = 0.01
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	# Провести дискуссию по национальному вопросу 
	moderate_discuss_minorities = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
			
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = did_discuss_fraction_articales
		}
		cost = 10
		days_remove = 30
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			set_country_flag = did_discuss_minorities
			add_popularity = {
				ideology = social_liberalism
				popularity = 0.01
			}
		}
	}
	moderate_discuss_minorities_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = social_liberalism
					popularity = 0.01
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	# Провести дискуссию по рабочему вопросу 
	moderate_discuss_workers = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
			
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = did_discuss_fraction_articales
		}
		cost = 10
		days_remove = 30
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			set_country_flag = did_discuss_workers
			add_popularity = {
				ideology = social_liberalism
				popularity = 0.01
			}
		}
	}
	moderate_discuss_workers_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = social_liberalism
					popularity = 0.01
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	# Устранить радикалов из Съезда 
	moderate_kill_radicals = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
			NOT = { has_country_flag = did_faction_peace }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = did_discuss_minorities
			has_country_flag = did_discuss_agrarian_reform
			has_country_flag = did_discuss_workers
		}
		cost = 25
		days_remove = 25
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			set_country_flag = did_kill_radicals
			add_popularity = {
				ideology = market_liberalism
				popularity = -0.03
			}
			add_popularity = {
				ideology = radical_socialism
				popularity = -0.03
			}
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
		}
	}
	moderate_kill_radicals_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 25
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = -0.03
				}
				add_popularity = {
					ideology = radical_socialism
					popularity = -0.03
				}
				add_to_variable = {
					var = RSS_party_antagonism
					value = 1
				}
				RSS_update_party_antagonism = yes
				moderate_makes_move_ai = yes
			}
		}
	}
	# Инициировать примирение платформ 
	moderate_faction_peace = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
			NOT = { has_country_flag = did_kill_radicals }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = did_discuss_minorities
			has_country_flag = did_discuss_agrarian_reform
			has_country_flag = did_discuss_workers
		}
		cost = 40
		days_remove = 35
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			set_country_flag = did_faction_peace
			add_popularity = {
				ideology = social_liberalism
				popularity = 0.03
			}
			add_to_variable = {
				var = RSS_party_antagonism
				value = -1
			}
			RSS_update_party_antagonism = yes
		}
	}
	moderate_faction_peace_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 35
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = social_liberalism
					popularity = 0.03
				}
				add_to_variable = {
					var = RSS_party_antagonism
					value = -1
				}
				RSS_update_party_antagonism = yes
				moderate_makes_move_ai = yes
			}
		}
	}
	# Подтвердить авторитет ЦК 
	moderate_central_comm_authority = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			OR = {
				has_country_flag = did_kill_radicals
				has_country_flag = did_faction_peace
			}
		}
		cost = 30
		days_remove = 20
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			set_country_flag = did_central_comm_authority
			add_popularity = {
				ideology = social_liberalism
				popularity = 0.03
			}
			add_stability = 0.03
		}
	}
	moderate_central_comm_authority_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 20
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = social_liberalism
					popularity = 0.03
				}
				add_stability = 0.03
				moderate_makes_move_ai = yes
			}
		}
	}
	# Официально распустить платформы 
	moderate_dismiss_platforms = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = did_central_comm_authority
		}
		cost = 30
		days_remove = 15
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			set_country_flag = did_dismiss_platforms
			add_popularity = {
				ideology = market_liberalism
				popularity = -0.03
			}
			add_popularity = {
				ideology = radical_socialism
				popularity = -0.03
			}
		}
	}
	moderate_dismiss_platforms_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 20
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = -0.03
				}
				add_popularity = {
					ideology = radical_socialism
					popularity = -0.03
				}
				moderate_makes_move_ai = yes
			}
		}
	}
	# Запретить фракционную борьбу 
	moderate_abolish_fractionism = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 }
			custom_trigger_tooltip = {
				tooltip = party_pop_most_sl
				check_variable = { party_popularity_100@social_liberalism > party_popularity_100@radical_socialism }
				check_variable = { party_popularity_100@social_liberalism > party_popularity_100@market_liberalism }
			}
		}
		visible = { 
			has_country_flag = RSS_esser_choose_soc_lib_side
			has_country_flag = did_dismiss_platforms
			has_country_flag = did_uncover_black_red_union
		}
		cost = 100
		days_remove = 56
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			country_event = rcw_rss_esser.58
		}
	}
	moderate_abolish_fractionism_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 56
		cancel_trigger = { hidden_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } } }
		timeout_effect = {
			hidden_effect = {
				country_event = rcw_rss_esser.58
				moderate_makes_move_ai = yes
			}
		}
	}
	###############
	# Блок Правых #
	###############
	# Програмная статья
		# максималистского искажения
	market_lib_programm_against_maximalist = {
		icon = eng_trade_unions_support
		available = { 
			always = yes 
			NOT = { has_country_flag = market_lib_choose_program }
			check_variable = { RSS_patry_activity_count < 1 }
			NOT = { has_country_flag = market_lib_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			NOT = { has_country_flag = market_lib_choose_program_start }
			NOT = { has_country_flag = market_lib_choose_program }
		}
		cost = 10
		days_remove = 7
		complete_effect = {
			set_country_flag = market_lib_choose_program_start
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_popularity = {
				ideology = radical_socialism
				popularity = -0.01
			}
			set_country_flag = market_lib_choose_program
			clr_country_flag = market_lib_choose_program_start
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	market_lib_programm_against_maximalist_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 7
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = radical_socialism
					popularity = -0.01
				}
				set_country_flag = market_lib_choose_program
				market_lib_makes_move_ai = yes
			}
		}
	}
		# умеренного искажения
	market_lib_programm_against_moderate = {
		icon = eng_trade_unions_support
		available = { 
			always = yes 
			NOT = { has_country_flag = market_lib_choose_program }
			check_variable = { RSS_patry_activity_count < 1 }
			NOT = { has_country_flag = market_lib_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			NOT = { has_country_flag = market_lib_choose_program_start }
			NOT = { has_country_flag = market_lib_choose_program }
		}
		cost = 10
		days_remove = 7
		complete_effect = {
			set_country_flag = market_lib_choose_program_start
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_popularity = {
				ideology = social_liberalism
				popularity = -0.01
			}
			set_country_flag = market_lib_choose_program
			clr_country_flag = market_lib_choose_program_start
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	market_lib_programm_against_moderate_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 7
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = social_liberalism
					popularity = -0.01
				}
				set_country_flag = market_lib_choose_program
				market_lib_makes_move_ai = yes
			}
		}
	}
	# Органзовать митинги сторонников
	market_lib_organise_rally = {
		icon = generic_political_rally
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = market_lib_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_choose_program
		}
		cost = 40
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_popularity = {
				ideology = market_liberalism
				popularity = 0.05
			}
			set_country_flag = market_lib_organised_rally_once
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}	
	}
	market_lib_organise_rally_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
				ideology = market_liberalism
				popularity = 0.05
			}
				set_country_flag = market_lib_organised_rally_once
				market_lib_makes_move_ai = yes
			}
		}
	}
	# Провести съезд сторонников 
	market_lib_supporters_congress = {
		icon = eng_blackshirt_march
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = market_lib_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_organised_rally_once
		}
		cancel_trigger = {
			NOT = { has_country_flag = market_lib_supporters_congress  }
		}
		cost = 70
		days_remove = 20
		cancel_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
		complete_effect = {
			set_country_flag = market_lib_supporters_congress
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_popularity = {
				ideology = market_liberalism
				popularity = 0.05
			}
			clr_country_flag = market_lib_supporters_congress
			set_country_flag = market_lib_supporters_congress_once 
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	market_lib_supporters_congress_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 20
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
				ideology = market_liberalism
				popularity = 0.05
			}
				set_country_flag = market_lib_supporters_congress_once
				market_lib_makes_move_ai = yes
			}
		}
	}
	# Выдвинуть вопрос о верховной власти
	market_lib_question_of_power = {
		icon = eng_trade_unions_demand
		available = { 
			AND = {
				custom_trigger_tooltip = {
					tooltip = party_pop_most_ml
					check_variable = { party_popularity_100@market_liberalism > party_popularity_100@social_liberalism }
					check_variable = { party_popularity_100@market_liberalism > party_popularity_100@radical_socialism }
				}
				custom_trigger_tooltip = {
					tooltip = party_pop_45_tt
					check_variable = { party_popularity_100@market_liberalism > 45 }
				}
				check_variable = { RSS_patry_activity_count < 1 }
				NOT = { has_country_flag = market_lib_banned }
			}
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_supporters_congress_once
		}
		cost = 50
		days_remove = 7
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			set_country_flag = market_lib_ultimatum
			country_event = { id = rcw_rss_esser.7 }
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	market_lib_question_of_power_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 7
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			set_country_flag = market_lib_ultimatum
			country_event = { id = rcw_rss_esser.7 }
		}
	}
	# Агитация на заводах 
	market_lib_agitation_in_factories = {
		icon = eng_propaganda_campaigns
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = market_lib_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
		}
		cost = 30
		days_remove = 20
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_popularity = {
				ideology = market_liberalism
				popularity = 0.03
			}
			set_country_flag = market_lib_did_factories_agitation_once
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
		}
	}
	market_lib_agitation_in_factories_ai_mission = {
		icon = eng_propaganda_campaigns
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 20
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = 0.03
				}
				set_country_flag = market_lib_did_factories_agitation_once
				market_lib_makes_move_ai = yes
			}
		}
	}
	# Поддержать лояльные профсоюзы
	market_lib_support_loyal_unions = {
		icon = generic_factory
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = market_lib_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_did_factories_agitation_once
		}
		cost = 50
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_popularity = {
				ideology = market_liberalism
				popularity = 0.04
			}
			set_country_flag = market_lib_did_support_loyal_unions_once
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	market_lib_support_loyal_unions_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = 0.04
				}
				set_country_flag = market_lib_did_support_loyal_unions_once
				market_lib_makes_move_ai = yes
			}
		}
	}
	# Организовать забастовку
	market_lib_organize_strike = {
		icon = generic_political_rally
		available = {
			AND = {
				custom_trigger_tooltip = {
					tooltip = medium_party_antagonism_tt
					check_variable = { RSS_party_antagonism > 3 }
				}
				custom_trigger_tooltip = {
					tooltip = party_not_dominant_tt
					OR = {
						check_variable = { party_popularity_100@radical_socialism > party_popularity_100@market_liberalism }
						check_variable = { party_popularity_100@social_liberalism > party_popularity_100@market_liberalism }
					}
				}
				NOT = { has_idea = strikes_1 }
				NOT = { has_idea = strikes_2 }
				NOT = { has_idea = strikes_3 }
				NOT = { has_idea = RSS_prohibition_of_strikes_idea }
				NOT = { has_country_flag = preperation_to_the_strike }
				check_variable = { RSS_patry_activity_count < 1 }
				NOT = { has_country_flag = market_lib_banned }
			} 	
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_did_support_loyal_unions_once
			NOT = { has_country_flag = market_lib_did_strike_once } 
		}
		cost = 50
		days_remove = 30
		complete_effect = {
			set_country_flag = preperation_to_the_strike
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			clr_country_flag = preperation_to_the_strike
			set_country_flag = market_lib_did_strike_once 
			add_popularity = {
				ideology = market_liberalism
				popularity = 0.07
			}
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
			add_timed_idea = {
				idea = strikes_1
				days = 21
			}
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	market_lib_organize_strike_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				clr_country_flag = preperation_to_the_strike
				set_country_flag = market_lib_did_strike_once 
				add_popularity = {
					ideology = market_liberalism
					popularity = 0.07
				}
				add_to_variable = {
					var = RSS_party_antagonism
					value = 1
				}
				RSS_update_party_antagonism = yes
				add_timed_idea = {
					idea = strikes_1
					days = 21
				}
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Агитация среди крестьян
	market_lib_agitation_peasants = {
		icon = eng_propaganda_campaigns
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = market_lib_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
		}
		cost = 20
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_popularity = {
				ideology = market_liberalism
				popularity = 0.03
			}
			set_country_flag = market_lib_did_peasant_agitation_once
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	market_lib_agitation_peasants_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = 0.03
				}
				set_country_flag = market_lib_did_peasant_agitation_once
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Продвижение сторонников в союзы трудового крестьянства
	market_lib_support_peasant_unions = {
		icon = hol_inundate_water_lines
		available = { 
			has_completed_focus = RSS_unions_of_the_labor_peasantry
			check_variable = { RSS_patry_activity_count < 1 }
			NOT = { has_country_flag = market_lib_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_did_peasant_agitation_once
		}
		cost = 50
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_popularity = {
				ideology = market_liberalism
				popularity = 0.06
			}
			set_country_flag = market_lib_support_peasant_unions_once
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	market_lib_support_peasant_unions_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = 0.06
				}
				set_country_flag = market_lib_support_peasant_unions_once
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Шантажировать город поставками
	market_lib_blackmail_the_city_with_supplies = {
		icon = hol_attract_foreign_investors
		available = {
			AND = {
				custom_trigger_tooltip = {
					tooltip = medium_plus_party_antagonism_tt
					check_variable = { RSS_party_antagonism > 4 }
				}
				check_variable = { RSS_patry_activity_count < 1 }
				custom_trigger_tooltip = {
					tooltip = party_pop_20_not_dom_tt
					AND = {
						check_variable = { party_popularity_100@radical_socialism > party_popularity_100@market_liberalism }
						check_variable = { party_popularity_100@market_liberalism > 20 }
					}
				}
				NOT = { has_country_flag = market_lib_banned }
			} 	
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_support_peasant_unions_once
			NOT = { has_country_flag = market_lib_did_blackmail_once }
		}
		cost = 50
		days_remove = 14
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
			set_country_flag = market_lib_did_blackmail
			set_country_flag = market_lib_did_blackmail_once		
			hidden_effect = {
				random_list = {
					40 = { set_country_flag = bread_good_answer }
					30 = { set_country_flag = bread_middle_answer }
					30 = { set_country_flag = bread_bad_answer }
				}
			}
			country_event = { id = rcw_rss_esser.8 }
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
		}
	}
	market_lib_blackmail_the_city_with_supplies_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 14
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_to_variable = {
					var = RSS_party_antagonism
					value = 1
				}
				RSS_update_party_antagonism = yes
				set_country_flag = market_lib_did_blackmail
				set_country_flag = market_lib_did_blackmail_once
				if = {
					limit = { has_country_flag = RSS_esser_choose_soc_lib_side }
					country_event = { id = rcw_rss_esser.9 }
				}
				else_if = {
					limit = { NOT = { has_country_flag = RSS_esser_choose_soc_lib_side } }
					random_list = {
						40 = {
							add_popularity = {
								ideology = market_liberalism
								popularity = 0.07
							} 
						}
						30 = { 
							add_popularity = {
								ideology = market_liberalism
								popularity = 0.04
							}
							random_list = {
								60 = {}
								40 = { rise_foodstuffs_supply_crisis = yes }
							}
						}
						30 = {  
							add_stability = -0.05
							rise_foodstuffs_supply_crisis = yes
						}
					}
				}
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Агитация в армии
	market_lib_agitation_in_army = {
		icon = eng_propaganda_campaigns
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = market_lib_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
		}
		cost = 30
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			if = {
				limit = { has_country_flag = market_lib_failed_conversation }
				add_days_remove  = {
					decision = market_lib_agitation_in_army
					days = 20
				}
			}
			if = {
				limit = { has_country_flag = politics_in_army_banned }
				add_days_remove  = {
					decision = market_lib_agitation_in_army
					days = 15
				}
			}
		}
		remove_effect = {
			add_war_support = -0.03
			add_popularity = {
				ideology = market_liberalism
				popularity = 0.03
			}
			set_country_flag = market_lib_did_army_agitation_once
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
		}
	}
	market_lib_agitation_in_army_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_war_support = -0.03
				add_popularity = {
					ideology = market_liberalism
					popularity = 0.03
				}
				set_country_flag = market_lib_did_army_agitation_once
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Разослать лояльных комиссаров
	market_lib_send_loyal_coms = {
		icon = generic_speech
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = market_lib_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_did_army_agitation_once
		}
		fire_only_once = yes
		cost = 70
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			if = {
				limit = { has_country_flag = market_lib_failed_conversation }
				add_days_remove  = {
					decision = market_lib_send_loyal_coms
					days = 20
				}
			}
			if = {
				limit = { has_country_flag = politics_in_army_banned }
				add_days_remove  = {
					decision = market_lib_send_loyal_coms
					days = 15
				}
			}			
		}
		remove_effect = {
			set_country_flag = market_lib_did_send_coms
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			add_popularity = {
				ideology = market_liberalism
				popularity = 0.03
			}
		}
	}
	market_lib_send_loyal_coms_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = { 
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = 0.03
				}
				set_country_flag = market_lib_did_send_coms
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Создать подпольную сеть
	market_lib_create_secret_web = {
		icon = generic_speech
		fire_only_once = yes
		available = { check_variable = { RSS_patry_activity_count < 1 } NOT = { has_country_flag = market_lib_banned } }
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_did_army_agitation_once
		}
		fire_only_once = yes
		cost = 70
		days_remove = 30
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			if = {
				limit = { has_country_flag = market_lib_failed_conversation }
				add_days_remove  = {
					decision = market_lib_create_secret_web
					days = 20
				}
			}	
		}
		remove_effect = {
			set_country_flag = market_lib_created_secret_web
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
		}
	}
	market_lib_create_secret_web_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 30
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = market_lib_created_secret_web
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Инспирировать устранение противников
		#Умереных
	market_lib_start_killing_moderate = {
		icon = generic_assassination
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			custom_trigger_tooltip = {
				tooltip = high_party_antagonism_tt
				check_variable = { RSS_party_antagonism > 6 }
			}
			NOT = { has_idea = moderate_faction_assassinations }
			NOT = { has_country_flag = market_lib_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_created_secret_web
		}
		cost = 50
		days_remove = 28
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			if = {
				limit = { has_country_flag = market_lib_failed_conversation }
				add_days_remove  = {
					decision = market_lib_start_killing_moderate
					days = 20
				}
			}	
		}
		remove_effect = {
			add_ideas = moderate_faction_assassinations 
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
		}
	}
	market_lib_start_killing_moderate_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 28
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_ideas = moderate_faction_assassinations 
				add_to_variable = {
					var = RSS_party_antagonism
					value = 1
				}
				RSS_update_party_antagonism = yes
				market_lib_makes_move_ai = yes
			}
		}
	}
		#Максималистов
	market_lib_start_killing_maximalist = {
		icon = generic_assassination
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			custom_trigger_tooltip = {
				tooltip = high_party_antagonism_tt
				check_variable = { RSS_party_antagonism > 6 }
			}
			NOT = { has_idea = maximalist_faction_assassinations }
			NOT = { has_country_flag = market_lib_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_created_secret_web
		}
		cost = 50
		days_remove = 28
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			if = {
				limit = { has_country_flag = market_lib_failed_conversation }
				add_days_remove  = {
					decision = market_lib_start_killing_maximalist
					days = 20
				}
			}
		}
		remove_effect = {
			add_ideas = maximalist_faction_assassinations 
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
		}
	}
	market_lib_start_killing_maximalist_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 28
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_ideas = maximalist_faction_assassinations  
				add_to_variable = {
					var = RSS_party_antagonism
					value = 1
				}
				RSS_update_party_antagonism = yes
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Подготовить склады с оружием
	market_lib_prepare_warehouse = {
		icon = generic_assassination
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			custom_trigger_tooltip = {
				tooltip = high_minus_party_antagonism_tt
				check_variable = { RSS_party_antagonism > 5 }
			}
			has_equipment = {
				infantry_equipment > 399
			}
			NOT = { has_country_flag = market_lib_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_created_secret_web
			has_country_flag = market_lib_did_send_coms
		}
		cost = 80
		days_remove = 35
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}
			if = {
				limit = { has_country_flag = market_lib_failed_conversation }
				add_days_remove  = {
					decision = market_lib_prepare_warehouse
					days = 20
				}
			}
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -400
			}
			set_country_flag = market_lib_prepared_warehouse
		}
	}
	market_lib_prepare_warehouse_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 35
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = market_lib_prepared_warehouse
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = -400
				}
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Вооруженная демонстрация 
	market_lib_arrest_gov = {
		icon = generic_assassination
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			custom_trigger_tooltip = {
				tooltip = high_plus_party_antagonism_tt
				check_variable = { RSS_party_antagonism > 7 }
			}
			has_stability < 0.4
			NOT = { has_country_flag = market_lib_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_prepared_warehouse
			has_country_flag = market_lib_did_send_coms
		}
		cost = 50
		days_remove = 21
		cancel_trigger = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on } }
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}	
			if = {
				limit = { has_country_flag = market_lib_failed_conversation }
				add_days_remove  = {
					decision = market_lib_arrest_gov
					days = 20
				}
			}
			activate_mission = sus_market_lib_rally_mission
			set_country_flag = market_lib_arrest_gov_start
		}
		remove_effect = {
			set_country_flag = market_lib_arrest_gov
			clr_country_flag = market_lib_arrest_gov_start
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}
			country_event = { id = rcw_rss_esser.23 }			
		}
	}
	market_lib_arrest_gov_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 21
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = market_lib_arrest_gov
				clr_country_flag = market_lib_arrest_gov_start
				country_event = { id = rcw_rss_esser.23 }
			}
		}
	}
	#Предложить коалицию левым кадетам 
	market_lib_coalition_with_cadets = {
		icon = generic_nationalism
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			NOT = { has_country_flag = market_lib_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
		}
		fire_only_once = yes
		cost = 40
		days_remove = 5
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			add_popularity = {
				ideology = market_liberalism
				popularity = 0.03
			}
			country_event = { id = rcw_rss_esser.18 }
		}
	}
	market_lib_coalition_with_cadets_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 5
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = market_lib_coalition_with_cadets
				add_popularity = {
					ideology = market_liberalism
					popularity = 0.03
				}
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Воспользоваться связями кадетов 
	market_lib_cadet_ties = {
		icon = generic_nationalism
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			NOT = { has_country_flag = market_lib_used_cadet_ties }
			NOT = { has_country_flag = market_lib_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_coalition_with_cadets
			NOT = { has_country_flag = market_lib_used_cadet_ties }
		}
		fire_only_once = yes
		cost = 40
		days_remove = 14
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			add_popularity = {
				ideology = market_liberalism
				popularity = 0.03
			}
			set_country_flag = market_lib_used_cadet_ties
		}
	}
	market_lib_cadet_ties_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 14
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = 0.03
				}
				set_country_flag = market_lib_used_cadet_ties
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Опереться на кадетскую прессу 
	market_lib_cadet_press = {
		icon = generic_nationalism
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			NOT = { has_country_flag = market_lib_used_cadet_press }
			NOT = { has_country_flag = market_lib_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_coalition_with_cadets
			NOT = { has_country_flag = market_lib_used_cadet_press }
		}
		fire_only_once = yes
		cost = 70
		days_remove = 35
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			add_popularity = {
				ideology = market_liberalism
				popularity = 0.05
			}
			set_country_flag = market_lib_used_cadet_press
		}
	}
	market_lib_cadet_press_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 35
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = 0.05
				}
				set_country_flag = market_lib_used_cadet_press
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Подготовить совместные митинги эсеров и кадетов 
	market_lib_cadet_rally = {
		icon = generic_nationalism
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			NOT = { has_country_flag = market_lib_used_cadet_rally }
			NOT = { has_country_flag = market_lib_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_coalition_with_cadets
			NOT = { has_country_flag = market_lib_used_cadet_rally }
		}
		fire_only_once = yes
		cost = 40
		days_remove = 14
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			add_popularity = {
				ideology = market_liberalism
				popularity = 0.03
			}
			set_country_flag = market_lib_used_cadet_rally
		}
	}
	market_lib_cadet_rally_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 14
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_popularity = {
					ideology = market_liberalism
					popularity = 0.03
				}
				set_country_flag = market_lib_used_cadet_rally
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Подготовить общую программу 
	market_lib_cadet_unite = {
		icon = generic_nationalism
		available = { 
			AND = {
				check_variable = { RSS_patry_activity_count < 1 } 
				has_country_flag = market_lib_used_cadet_rally 
				has_country_flag = market_lib_used_cadet_press
				has_country_flag = market_lib_used_cadet_ties
				NOT = { has_country_flag = market_lib_banned }
			}
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_coalition_with_cadets
		}
		fire_only_once = yes
		cost = 50
		days_remove = 28
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			country_event = { id = rcw_rss_esser.19 }
		}
	}
	market_lib_cadet_unite_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 28
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = RSS_cadets_esser_united
				random_list = {
					50 = {
						add_popularity = {
							ideology = market_liberalism
							popularity = 0.05
						}
					}
					50 = { 
						add_popularity = {
							ideology = market_liberalism
							popularity = 0.03
						}
					}
				}
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Номинальный представитель 
	market_lib_nominal = {
		icon = generic_nationalism
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			NOT = { has_country_flag = market_lib_banned }
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = RSS_cadets_esser_united
		}
		fire_only_once = yes
		cost = 20
		days_remove = 14
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			country_event = { id = rcw_rss_esser.20 }
		}
	}
	market_lib_nominal_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 14
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = RSS_set_nominal
				random_list = {
					33 = {
						add_popularity = {
							ideology = market_liberalism
							popularity = 0.03
						}
					}
					33 = { 
						add_popularity = {
							ideology = market_liberalism
							popularity = 0.03
						}
						create_country_leader = { name = "Vladimir Vinogradov" desc = "" picture = "gfx/leaders/RUS/Portrait_Vladimir_Vinogradov.dds" expire = "1932.12.03" ideology = market_liberalism_ideology traits = { } }
					}
					33 = {
						modifier = {
							factor = 0
							NOT = { has_country_flag = Sergey_Maslov_is_here }
						}
						add_popularity = {
							ideology = market_liberalism
							popularity = 0.03
						}
						create_country_leader = { name = "Sergey Maslov" desc = "" picture = "gfx/leaders/RUS/Portrait_Sergey_Maslov.dds" expire = "1932.12.03" ideology = market_liberalism_ideology traits = { } }
						if = {
							limit = { check_variable = { party_popularity_100@market_liberalism < 15 } }
							add_popularity = {
								ideology = market_liberalism
								popularity = 0.05
							}
							add_stability = -0.04
						}
					}
				}
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Беседа с новым главкомом
	market_lib_conversation_with_army_chief = {
		icon = generic_army_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			NOT = { has_country_flag = army_chief_conversation_happened }
			NOT = { has_country_flag = market_lib_banned }
			OR = {
				has_idea = Radola_Gaida_army_chief
				has_idea = Fyodor_Makhin_army_chief
				has_idea = Vasiliy_Boldyrev_army_chief
			}
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			NOT = { has_country_flag = army_chief_conversation_happened }
		}
		cost = 5
		days_remove = 5
		fire_only_once = yes
		complete_effect = {
			set_country_flag = army_chief_conversation_happened
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			set_country_flag = market_lib_did_conversation
			country_event = { id = rcw_rss_esser.10 }
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
		}
	}
	market_lib_conversation_with_army_chief_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 5
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				set_country_flag = market_lib_did_conversation
				if = {
					limit = { has_idea = Fyodor_Makhin_army_chief }
					random_list = {
						25 = { set_country_flag = market_lib_army_chief_recruted }
						75 = { 
							random_list = {
								50 = { 
									random_list = {
										60 = { country_event = { id = rcw_rss_esser.13 } }
										40 = { country_event = { id = rcw_rss_esser.14 } }
									}
								}
								50 = { country_event = { id = rcw_rss_esser.14 } }
							}
						}
					}
				}
				else_if = {
					limit = { has_idea = Radola_Gaida_army_chief }
					random_list = {
						70 = { set_country_flag = market_lib_army_chief_recruted }
						30 = { 
							random_list = {
								50 = { 
									random_list = {
										60 = { country_event = { id = rcw_rss_esser.13 } }
										40 = { country_event = { id = rcw_rss_esser.14 } }
									}
								}
								50 = { country_event = { id = rcw_rss_esser.14 } }
							} 
						}
					}
				}
				else_if = {
					limit = { has_idea = Vasiliy_Boldyrev_army_chief }
					random_list = {
						10 = {
							set_country_flag = market_lib_army_chief_recruted
							add_popularity = {
								ideology = national_syndicalism
								popularity = 0.03
							}
						}
						90 = { 
							random_list = {
								50 = { 
									random_list = {
										60 = { country_event = { id = rcw_rss_esser.13 } }
										40 = { country_event = { id = rcw_rss_esser.14 } }
									}
								}
								50 = { country_event = { id = rcw_rss_esser.14 } }
							}
						}
					}
				}
				market_lib_makes_move_ai = yes
			}
		}
	}
	#Объявить «народовцев» немецкими агентами 
	declare_maximalist_german_agents = {
		icon = eng_trade_unions_support
		available = { 
			check_variable = { RSS_patry_activity_count < 1 } 
			
		}
		visible = { 
			has_country_flag = RSS_esser_choose_market_lib_side
			has_country_flag = market_lib_used_cadet_press
		}
		cost = 5
		days_remove = 14
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		fire_only_once = yes
		complete_effect = {
			add_to_variable = {
				var = RSS_patry_activity_count
				value = 1
			}			
		}
		remove_effect = {
			add_to_variable = {
				var = RSS_party_antagonism
				value = 1
			}
			RSS_update_party_antagonism = yes
			country_event = { id = rcw_rss_esser.56 }
			add_to_variable = {
				var = RSS_patry_activity_count
				value = -1
			}	
			set_country_flag = declared_maximalist_german_agents
		}
	}
	declare_maximalist_german_agents_ai_mission = {
		activation = { hidden_trigger = { always = no } }
		available = { hidden_trigger = { always = no } }
		visible = { hidden_trigger = { always = no } }
		days_mission_timeout = 14
		cancel_trigger = { hidden_trigger = { OR = { NOT = { has_country_flag = RSS_esser_party_struggle_is_on }  has_country_flag = market_lib_banned } } }
		timeout_effect = {
			hidden_effect = {
				add_to_variable = {
					var = RSS_party_antagonism
					value = 1
				}
				RSS_update_party_antagonism = yes
				country_event = { id = rcw_rss_esser.56 }
				market_lib_makes_move_ai = yes
			}
		}
	}
}