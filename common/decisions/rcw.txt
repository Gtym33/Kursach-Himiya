brest_litovsk_negotiations = {
	
	UKR_send_delegation = {

		icon = generic_form_nation

		allowed = {
			original_tag = UKR
			NOT = {
				has_global_flag = UKR_delegation_on_brest_litovsk_negotiations 
				has_government = radical_socialism
				has_government = communism
			}
		}

		available = {
			has_war_with = GER
		}
		complete_effect = {
			GER = { country_event = treaty_of_brest.10 }
		}

		ai_will_do = {
			factor = 200
			modifier = {
				factor = 0
				NOT = { controls_state = 202 }
			}
		}
	}
	
	make_peace = {

		icon = generic_form_nation

		allowed = {
			original_tag = RUS
		}

		available = {
			has_war_with = GER
		}
		complete_effect = {
			GER = {
				if = {
					limit = { has_global_flag = brest_litovsk_negotiations_extended_GER_requirements_2 }
					country_event = treaty_of_brest.11
					else = {
						if = {
							limit = { has_global_flag = brest_litovsk_negotiations_extended_GER_requirements_1 }
							country_event = treaty_of_brest.12
							else = {
								country_event = treaty_of_brest.13
							}
						}
					}
				}
			}
		}

		ai_will_do = {
			factor = 0
		}
	}
	
	GER_make_peace_UKR = {

		icon = generic_form_nation

		allowed = {
			original_tag = GER
		}
		days_remove = 60

		available = {
			has_war_with = UKR
			has_global_flag = UKR_delegation_on_brest_litovsk_negotiations
			UKR = {
				controls_state = 202
			}
		}
		remove_effect = {
			UKR = {
				country_event = treaty_of_brest.14
			}
		}

		ai_will_do = {
			factor = 200
			modifier = {
				factor = 0
				is_historical_focus_on = yes
			}
		}
	}
	
	GER_ultimatum_to_sovnarkom = {

		icon = generic_form_nation

		allowed = {
			original_tag = GER
		}
		days_remove = 30

		available = {
			has_war_with = RUS
			has_global_flag = brest_litovsk_negotiations_extended_GER_requirements_2
		}
		remove_effect = {
			RUS = {
				country_event = treaty_of_brest.17
			}
		}

		ai_will_do = {
			factor = 200
		}
	}
}