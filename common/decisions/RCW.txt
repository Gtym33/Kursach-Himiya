
rcw_intervention_desisions = {
	
	intervention_in_Arkhangelsk = {

		icon = generic_prepare_civil_war

		available = {
			check_variable = { RUS_threat_of_intervention > 1 }
			any_country = {
				OR = {
					tag = RUS
					is_in_faction_with = GER
					is_in_faction_with = AUH
				}
				214 = {
					is_controlled_by = PREV
				}
			}
		}
		visible = {
			original_tag = ENG
		}

		cost = 25
		ai_will_do = {
			factor = 500
		}
		days_remove = 30

		remove_effect = {
		}
		complete_effect = {
		}
	}
	
	intervention_in_Murmansk = {

		icon = generic_prepare_civil_war

		available = {
			check_variable = { RUS_threat_of_intervention > 3 }
			any_country = {
				OR = {
					tag = RUS
					is_in_faction_with = GER
					is_in_faction_with = AUH
				}
				213 = {
					is_controlled_by = PREV
				}
			}
		}
		visible = {
			original_tag = ENG
		}

		cost = 25
		ai_will_do = {
			factor = 500
		}
		days_remove = 30

		remove_effect = {
		}
		complete_effect = {
		}
	}
	
	break_the_peace_with_Makhno = {

		icon = generic_prepare_civil_war

		available = {
			has_country_flag = RUS_soviet_power_flag
			custom_trigger_tooltip = {
				tooltip = RUS_party_power_control_more_then_4
				check_variable = { RUS_party_power_control > 4 }
			}
		}
		visible = {
			has_country_flag = RUS_soviet_power_flag
			FRT = {
				exists = yes
				is_in_faction_with = ROOT
				NOT = { has_government = communism }
			}
		}

		cost = 50
		ai_will_do = {
			factor = 0
			modifier = {
				factor = 0
			}
		}
		days_remove = 50
		fire_only_once = yes
		war_with_on_remove = GEO

		remove_effect = {
			declare_war_on = { target = FRT type = annex_everything }
		}
		complete_effect = {
			add_to_variable = { var = RUS_soviet_power_control value = -1 }
			add_to_variable = { var = RUS_party_power_control value = 1 }
		}
	}
}