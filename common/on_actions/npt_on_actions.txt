on_actions = {

	on_annex = {
		effect = {
		if = { 
		limit = {
		ROOT = { is_white_movement_country = yes } 
		FROM = { tag = PRM }
		}
		PRM = { every_unit_leader = { set_nationality = ROOT } } 
		}
		}
	}
}