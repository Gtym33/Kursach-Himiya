on_actions = {
	on_capitulation = {
		effect = {
			IF = {
				limit = {
					NOT = { has_global_flag = RUS_civil_war_over }
					tag = BLR
					has_country_flag = RCW_exiled_gov
					has_war_with = RUS
					NOT = { has_war_with = LIT }
				}
				become_exiled_in = {
					target = LIT
					legitimacy = 5
				}
			}
		}
	}
}