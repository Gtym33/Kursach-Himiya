EMB = {	#Embargoed nation
	target_array = ROOT.embargoed_countries
	country_score = {
		base = 0
		modifier = {
			add = 10
			has_country_flag = this_remove_embargo
		}
	}
}

UNR = {	#Unrecognized state
	target_array = ROOT.unrecognized_states
	country_score = {
		base = 0
		modifier = {
			add = 10
			has_country_flag = this_remove_unrecognizing
		}
	}
}
