white_movement_countries_not_exists = {
	any_other_country = { limit = { tag = CCA } OR = { has_war = no exists = no } }
	any_other_country = { limit = { tag = DON } OR = { has_war = no exists = no } }
	any_other_country = { limit = { tag = RVA } OR = { has_war = no exists = no } }
	any_other_country = { limit = { tag = RSS } OR = { has_war = no exists = no } }
	any_other_country = { limit = { tag = NRR } OR = { has_war = no exists = no } }
	any_other_country = { limit = { tag = PRM } OR = { has_war = no exists = no } }
	any_other_country = { limit = { tag = BKL } OR = { has_war = no exists = no } }
	any_other_country = { limit = { tag = KHI } OR = { has_war = no exists = no } }
	any_other_country = { limit = { tag = BUK } OR = { has_war = no exists = no } }
	any_other_country = { limit = { tag = TRK } OR = { has_war = no exists = no } }
	any_other_country = { limit = { tag = SIB } OR = { has_war = no exists = no } }
}
is_white_movement_country = {
	OR = {
		tag = CCA
		tag = DON
		tag = RVA
		tag = RSS
		tag = NRR
		tag = PRM
		tag = BKL
		tag = KHI
		tag = BUK
		tag = TRK
		tag = SIB
	}
}