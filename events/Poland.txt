add_namespace = poland

# Объявление войны Белоруссии
country_event = {
	id = poland.0
	title = poland.0.t
	desc = poland.0.desc
	picture = GFX_report_event_greece_civil_war_soldiers
	
	trigger = {
		has_start_date < 1919.01.01
		tag = POL
		is_subject = no
		NOT = { has_war_with = BLR }
		is_neighbor_of = BLR
	}

	mean_time_to_happen = {
		days = 0
	}
	
	fire_only_once = yes
	
	option = {
		name = poland.0.a
		declare_war_on = {
			target = BLR
			type = annex_everything
		}
		if = {
			limit = { BLR = { has_war_with = RSR } }
			declare_war_on = {
				target = RSR
				type = annex_everything
			}
		}
	}
}

# Независимость Польши после поражения Германии в ПМВ
country_event = {
	id = poland.1
	title = poland.1.t
	desc = poland.1.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	fire_only_once = yes
	
	trigger = {
		has_start_date < 1918.11.11
		tag = POL
		OR = {
			has_global_flag = WWI_GER_HAS_CAPITULATED
			GER = { has_country_flag = GER_monarchy_fall }
		}
		is_subject = no
		NOT = { has_country_leader = { name = "Józef Pilsudski" } }
	}

	mean_time_to_happen = {
		days = 0
	}
	
	option = {
		name = poland.1.a
		drop_cosmetic_tag = yes
		add_popularity = { ideology = authoritarism popularity = -1 }
		if = {
			limit = { has_country_leader = { name = "Hans Beseler" ruling_only = yes } }
			retire_country_leader = yes
		}
		add_popularity = { ideology = social_democracy popularity = 0.1 }
		set_politics = { ruling_party = despotism elections_allowed = no }
		if = {
			limit = { has_country_leader = { name = "Hans Beseler" ruling_only = yes } }
			retire_country_leader = yes
		}
		create_country_leader = { name = "Józef Pilsudski" desc = POL_jozef_pilsudski_DESC picture = "gfx/leaders/POL/Portrait_Jusef_Pilsudsky.dds" expire = "1935.1.1" ideology = despotism_ideology  traits = {  } }
		create_field_marshal = {  name = "Józef Pilsudski" id = 69053 portrait_path = "gfx/leaders/POL/Portrait_Jusef_Pilsudky_military.dds" traits = { politically_connected war_hero infantry_officer offensive_doctrine adaptable } skill = 3 attack_skill = 4 defense_skill = 1 planning_skill = 2 logistics_skill = 3 }
		load_oob = POL_1917
	}
}

# Захват ЗУНР
country_event = {
	id = poland.2
	title = poland.2.t
	desc = poland.2.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	fire_only_once = yes
	
	trigger = {
		tag = POL
		is_subject = no
		OR = {
			has_global_flag = WWI_GER_HAS_CAPITULATED
			GER = { has_country_flag = GER_monarchy_fall }
		}
		GAL = { exists = yes is_subject = no }
	}

	mean_time_to_happen = {
		days = 0
	}
	
	option = {
		name = poland.2.a
		ai_chance = {
			factor = 1
		}
		add_war_support = 0.05
		add_popularity = { ideology = despotism popularity = 0.1 }
		declare_war_on = {
				target = GAL
				type = annex_everything
			}
		hidden_effect = {
			GAL = {
				load_oob = GAL_defender
			}
		}
	}
	
	option = {
		name = poland.2.b
		ai_chance = {
			factor = 0
		}
		add_war_support = -0.1
		add_popularity = { ideology = despotism popularity = -0.3 }
	}
}

# ЗУНР отбилась
country_event = {
	id = poland.3
	title = poland.3.t
	desc = poland.3.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	fire_only_once = yes
	
	trigger = {
		tag = POL
		has_war_with = GAL
		GAL = { is_puppet = no }
		OR = {
			NOT = {
				controls_state = 10
			}
			has_capitulated = yes
			surrender_progress > 0.4
		}
	}

	mean_time_to_happen = {
		days = 0
	}
	
	option = {
		name = poland.3.a
		add_war_support = -0.3
		every_country = {
				limit = {
					has_war_with = GAL
					OR = {
						tag = POL
						is_in_faction_with = POL
					}
				}
				white_peace = GAL
		}
	}
	
}

# Конфликт с Литвой
country_event = {
	id = poland.4
	title = poland.4.t
	desc = poland.4.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	trigger = {
		has_start_date < 1919.01.01
		tag = POL
		is_subject = no
		NOT = { has_war_with = LIT }
		owns_state = 1001
		LIT = { owns_state = 97 }
	}

	mean_time_to_happen = {
		days = 0
	}
	
	fire_only_once = yes
	
	option = {
		name = poland.2.a
		start_border_war = {
			change_state_after_war = yes
			attacker = {
				state = 1001
				num_provinces = 2
				on_win = poland.6
				on_lose = poland.5
				on_cancel = poland.5
			}
			defender = {
				state = 97
				num_provinces = 3
				on_win = poland.5
				on_lose = poland.6
				on_cancel = poland.5
			}
		}
		ai_chance = {
			factor = 100
		}
	}
	option = {
		name = poland.2.b
		ai_chance = {
			factor = 0
		}
	}
}

# Поражение (остановка)
country_event = {
	id = poland.5
	title = poland.5.t
	desc = poland.5.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		trigger = {
			tag = POL
			LIT = { is_neighbor_of = ROOT }
		}
		name = poland.5.a
		declare_war_on = {
			target = LIT
			type = annex_everything
		}
		ai_chance = {
			factor = 100
		}
	}
	option = {
		name = poland.5.b
		ai_chance = {
			factor = 0
		}
	}
}
# Промежуточная победа
country_event = {
	id = poland.6
	title = poland.6.t
	desc = poland.6.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		name = poland.6.a
		if = {
			limit = { tag = POL }
			start_border_war = {
				change_state_after_war = yes
				attacker = {
					state = 97
					num_provinces = 3
					on_win = poland.7
					on_lose = poland.5
					on_cancel = poland.5
				}
				defender = {
					state = 956
					num_provinces = 3
					on_win = poland.5
					on_lose = poland.7
					on_cancel = poland.5
				}
			}
		}
	}
}
# Победа и переход в наступление на БНР
country_event = {
	id = poland.7
	title = poland.7.t
	desc = poland.7.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		trigger = {
			tag = POL
			BLR = { is_neighbor_of = ROOT }
		}
		name = poland.7.a
		declare_war_on = {
			target = BLR
			type = annex_everything
		}
		ai_chance = {
			factor = 100
		}
	}
	option = {
		name = poland.7.b
		ai_chance = {
			factor = 0
		}
	}
}

# Ультиматум Петлюре
country_event = {
	id = poland.8
	title = poland.8.t
	desc = poland.8.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	trigger = {
		tag = POL
		OR = {
			has_global_flag = WWI_GER_HAS_CAPITULATED
			GER = { has_country_flag = GER_monarchy_fall }
		}
		owns_state = 904
		owns_state = 1165
		GAL = { exists = no }
		any_country = {
			original_tag = UKR
			is_neighbor_of = POL
			any_other_country = { NOT = { former_russian_empire_ukraine_country = yes } has_war_with = PREV }
			surrender_progress > 0.5
		}
	}
	
	mean_time_to_happen = { days = 0 }
	
	fire_only_once = yes
	
	option = {
		ai_chance = {
			factor = 100
		}
		name = poland.8.a
		random_other_country = {
			limit = {
				original_tag = UKR
				is_neighbor_of = POL
				any_other_country = { NOT = { former_russian_empire_ukraine_country = yes } has_war_with = PREV }
				surrender_progress > 0.5
			}
			country_event = { id = poland.9 hours = 2 }
		}
	}
	option = {
		ai_chance = {
			factor = 0
		}
		name = poland.8.b
	}
}
# Ответ Польше (Украина)
country_event = {
	id = poland.9
	title = poland.9.t
	desc = poland.9.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	trigger = {
		tag = UKR
		is_subject = no
	}
	
	is_triggered_only = yes
	
	option = {
		trigger = { NOT = { has_country_flag = UKR_white has_government = communism has_government = radical_socialism } }
		name = poland.9.a
		add_stability = -0.01
		every_state = {
			limit = {
				is_owned_by = ROOT
				OR = {
					is_core_of = FROM
					is_claimed_by = FROM
				}
			}
			FROM = { set_state_owner = PREV }
		}
		every_country = {
			limit = { has_war_with = ROOT NOT = { has_war_with = FROM } }
			FROM = { declare_war_on = { target = PREV type = annex_everything } }
			if = {
				limit = { OR = { is_faction_leader = yes AND = { is_in_faction = no is_subject = no } } }
				country_event = poland.21 # уведомление о том, что Польша взяла Украину
			}
		}
		hidden_effect = {
			every_owned_state = {
				limit = { is_capital = no NOT = { is_fully_controlled_by = OWNER } }
				set_state_flag = transfered_state
				set_state_owner_to = FROM
			}
		}
		FROM = { ROOT = { subjecting_script_with_transfer_parties_colony = yes } }
		hidden_effect = {
			every_state = {
				limit = { has_state_flag = transfered_state }
				clr_state_flag = transfered_state
				set_state_owner_to = ROOT
			}
		}
		ai_chance = {
			factor = 100
		}
	}
	option = {
		name = poland.9.b
		FROM = { country_event = { id = poland.10 days = 1 } }
		ai_chance = {
			factor = 0
		}
	}
	option = {
		name = poland.9.c
		every_country = {
			limit = {
				former_russian_empire_ukraine_country = yes
				NOT = { has_war_with = ROOT }
			}
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
		}
		ai_chance = {
			factor = 0
		}
	}
}

# Польша реагирует на неуступчивость Украины
country_event = {
	id = poland.10
	title = poland.10.t
	desc = poland.10.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		ai_chance = {
			factor = 0
		}
		name = poland.10.a
	}
	option = {
		ai_chance = {
			factor = 100
		}
		name = poland.10.b
		every_country = {
			limit = {
				former_russian_empire_ukraine_country = yes
				NOT = { has_war_with = ROOT }
			}
			ROOT = {
				declare_war_on = {
					target = PREV
					type = annex_everything
				}
			}
		}
		if = {
			limit = { any_country = { former_russian_empire_ukraine_country = yes has_war_with = RUS } }
			declare_war_on = {
				target = RUS
				type = annex_everything
			}
		}
	}
}

# Польша или Россия предлагает другой стороне мир с большими уступками для Польши
country_event = {
	id = poland.11
	title = poland.11.t
	desc = poland.11.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		name = poland.11.a
		FROM = { country_event = poland.17 }
		ai_chance = {
			factor = 1
		}
	}
	option = {
		trigger = { ROOT = { original_tag = POL } }
		name = poland.11.b
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = FROM tag = ROOT } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_country = {
			limit = {
				is_subject_of = ROOT
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
				}
			}
			every_state = {
				limit = {
					OR = { OWNER = { OR = { tag = FROM is_subject_of = FROM } } CONTROLLER = { OR = { tag = FROM is_subject_of = FROM } } }
					OR = {
						is_core_of = PREV
						is_claimed_by = PREV
					}
				}
				PREV = { transfer_state = PREV }
			}
		}
		every_state = {
			limit = {
				OR = { OWNER = { OR = { tag = FROM is_subject_of = FROM } } CONTROLLER = { OR = { tag = FROM is_subject_of = FROM } } }
				OR = {
					is_core_of = ROOT
					is_claimed_by = ROOT
					is_core_of = BLR
					is_claimed_by = BLR
					is_core_of = LIT
					is_claimed_by = LIT
					is_core_of = COU
					is_claimed_by = COU
					is_core_of = UKR
					is_claimed_by = UKR
				}
			}
			ROOT = { transfer_state = PREV }
		}
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = FROM tag = ROOT } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		if = {
			limit = { LIT = { has_war_with = FROM } }
			every_country = {
				limit = { OR = { tag = FROM is_in_faction_with = FROM } }
				white_peace = LIT
			}
			every_state = {
				limit = {
					is_owned_by = FROM
					is_core_of = LIT
				}
				LIT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { LAT = { has_war_with = FROM } }
			every_country = {
				limit = { OR = { tag = FROM is_in_faction_with = FROM } }
				white_peace = LAT
			}
			every_state = {
				limit = {
					is_owned_by = FROM
					is_core_of = LAT
				}
				LAT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { EST = { has_war_with = FROM } }
			every_country = {
				limit = { OR = { tag = FROM is_in_faction_with = FROM } }
				white_peace = EST
			}
			every_state = {
				limit = {
					is_owned_by = FROM
					is_core_of = EST
				}
				EST = { transfer_state = PREV }
			}
		}
		every_country = {
			limit = { OR = { tag = FROM is_in_faction_with = FROM } }
			white_peace = ROOT
		}
		ai_chance = {
			factor = 2
		}
	}
	option = {
		trigger = { ROOT = { NOT = { original_tag = POL } } }
		name = poland.11.b
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = ROOT tag = FROM } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_country = {
			limit = {
				is_subject_of = FROM
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
				}
			}
			every_state = {
				limit = {
					OR = { OWNER = { OR = { tag = ROOT is_subject_of = ROOT } } CONTROLLER = { OR = { tag = ROOT is_subject_of = ROOT } } }
					OR = {
						is_core_of = PREV
						is_claimed_by = PREV
					}
				}
				PREV = { transfer_state = PREV }
			}
		}
		every_state = {
			limit = {
				OR = { OWNER = { OR = { tag = ROOT is_subject_of = ROOT } } CONTROLLER = { OR = { tag = ROOT is_subject_of = ROOT } } }
				OR = {
					is_core_of = FROM
					is_claimed_by = FROM
					is_core_of = BLR
					is_claimed_by = BLR
					is_core_of = LIT
					is_claimed_by = LIT
					is_core_of = COU
					is_claimed_by = COU
					is_core_of = UKR
					is_claimed_by = UKR
				}
			}
			FROM = { transfer_state = PREV }
		}
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = ROOT tag = FROM } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		if = {
			limit = { LIT = { has_war_with = ROOT } }
			every_country = {
				limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
				LIT = { white_peace = PREV }
			}
			every_state = {
				limit = {
					is_owned_by = ROOT
					is_core_of = LIT
				}
				LIT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { LAT = { has_war_with = ROOT } }
			every_country = {
				limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
				LAT = { white_peace = PREV }
			}
			every_state = {
				limit = {
					is_owned_by = ROOT
					is_core_of = LAT
				}
				LAT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { EST = { has_war_with = ROOT } }
			every_country = {
				limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
				EST = { white_peace = PREV }
			}
			every_state = {
				limit = {
					is_owned_by = ROOT
					is_core_of = EST
				}
				EST = { transfer_state = PREV }
			}
		}
		every_country = {
			limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
			FROM = { white_peace = PREV }
		}
		ai_chance = {
			factor = 2
		}
	}
}

# Польша или Россия предлагает другой стороне историчный мир
country_event = {
	id = poland.12
	title = poland.12.t
	desc = poland.12.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		name = poland.12.a
		FROM = { country_event = poland.17 }
		ai_chance = {
			factor = 1
		}
	}
	option = {
		trigger = { ROOT = { original_tag = POL } }
		name = poland.12.b
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = FROM tag = ROOT } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_state = {
			limit = {
				OR = {
					is_core_of = ROOT
					is_claimed_by = ROOT
					is_core_of = LIT
					is_claimed_by = LIT
				}
				OR = { OWNER = { OR = { tag = FROM is_subject_of = FROM } } CONTROLLER = { OR = { tag = FROM is_subject_of = FROM } } }
			}
			ROOT = { transfer_state = PREV }
		}
		every_state = {
			limit = {
				is_owned_by = ROOT
				OR = {
					state = 1004
					region = 37
					region = 130
					region = 131
					state = 206
					state = 924
					state = 1168
					state = 922
					state = 94
					state = 240
					region = 234
					region = 132
				}
				NOT = {
					is_core_of = ROOT
					is_claimed_by = ROOT
					is_core_of = LIT
					is_claimed_by = LIT
				}
			}
			FROM = { transfer_state = PREV }
		}
		every_country = {
			limit = {
				is_subject_of = ROOT
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_owned_state = {
				limit = {
					OR = {
						is_core_of = ROOT
						is_claimed_by = ROOT
						is_core_of = LIT
						is_claimed_by = LIT
					}
				}
				ROOT = { transfer_state = PREV }
			}
			ROOT = { end_puppet = PREV }
		}
		every_country = {
			limit = { OR = { tag = FROM is_in_faction_with = FROM } }
			white_peace = ROOT
		}
		if = {
			limit = { LIT = { has_war_with = FROM } }
			every_country = {
				limit = { OR = { tag = FROM is_in_faction_with = FROM } }
				white_peace = LIT
			}
			every_state = {
				limit = {
					is_owned_by = FROM
					is_core_of = LIT
				}
				LIT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { LAT = { has_war_with = FROM } }
			every_country = {
				limit = { OR = { tag = FROM is_in_faction_with = FROM } }
				white_peace = LAT
			}
			every_state = {
				limit = {
					is_owned_by = FROM
					is_core_of = LAT
				}
				LAT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { EST = { has_war_with = FROM } }
			every_country = {
				limit = { OR = { tag = FROM is_in_faction_with = FROM } }
				white_peace = EST
			}
			every_state = {
				limit = {
					is_owned_by = FROM
					is_core_of = EST
				}
				EST = { transfer_state = PREV }
			}
		}
		ai_chance = {
			factor = 4
		}
	}
	option = {
		trigger = { ROOT = { NOT = { original_tag = POL } } }
		name = poland.12.b
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = ROOT tag = FROM } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_state = {
			limit = {
				OR = {
					is_core_of = FROM
					is_claimed_by = FROM
					is_core_of = LIT
					is_claimed_by = LIT
				}
				OR = { OWNER = { OR = { tag = ROOT is_subject_of = ROOT } } CONTROLLER = { OR = { tag = ROOT is_subject_of = ROOT } } }
			}
			FROM = { transfer_state = PREV }
		}
		every_state = {
			limit = {
				is_owned_by = FROM
				OR = {
					state = 1004
					region = 37
					region = 130
					region = 131
					state = 206
					state = 924
					state = 1168
					state = 922
					state = 94
					state = 240
					region = 234
					region = 132
				}
				NOT = {
					is_core_of = FROM
					is_claimed_by = FROM
					is_core_of = LIT
					is_claimed_by = LIT
				}
			}
			ROOT = { transfer_state = PREV }
		}
		every_country = {
			limit = {
				is_subject_of = FROM
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_owned_state = {
				limit = {
					OR = {
						is_core_of = FROM
						is_claimed_by = FROM
						is_core_of = LIT
						is_claimed_by = LIT
					}
				}
				FROM = { transfer_state = PREV }
			}
			FROM = { end_puppet = PREV }
		}
		every_country = {
			limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
			FROM = { white_peace = PREV }
		}
		if = {
			limit = { LIT = { has_war_with = ROOT } }
			every_country = {
				limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
				LIT = { white_peace = PREV }
			}
			every_state = {
				limit = {
					is_owned_by = ROOT
					is_core_of = LIT
				}
				LIT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { LAT = { has_war_with = ROOT } }
			every_country = {
				limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
				LAT = { white_peace = PREV }
			}
			every_state = {
				limit = {
					is_owned_by = ROOT
					is_core_of = LAT
				}
				LAT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { EST = { has_war_with = ROOT } }
			every_country = {
				limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
				EST = { white_peace = PREV }
			}
			every_state = {
				limit = {
					is_owned_by = ROOT
					is_core_of = EST
				}
				EST = { transfer_state = PREV }
			}
		}
		ai_chance = {
			factor = 4
		}
	}
}

# Польша или Россия предлагает другой стороне мир (Польша замкнута в границах царства польского)
country_event = {
	id = poland.13
	title = poland.13.t
	desc = poland.13.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		name = poland.13.a #Отказ
		FROM = { country_event = poland.17 }
		ai_chance = {
			factor = 2
		}
	}
	option = {
		trigger = { ROOT = { original_tag = POL } }
		name = poland.13.b
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				is_subject_of = FROM
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_state = {
				limit = {
					OR = { OWNER = { OR = { tag = ROOT is_subject_of = ROOT } } CONTROLLER = { OR = { tag = ROOT is_subject_of = ROOT } } }
					OR = {
						is_core_of = PREV
						is_claimed_by = PREV
					}
				}
				PREV = { transfer_state = PREV }
			}
		}
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = FROM tag = ROOT } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_state = {
			limit = {
				is_owned_by = ROOT
				NOT = { is_core_of = ROOT }
			}
			FROM = { transfer_state = PREV }
		}
		every_state = {
			limit = {
				is_core_of = ROOT
				OR = { OWNER = { OR = { tag = FROM is_subject_of = FROM } } CONTROLLER = { OR = { tag = FROM is_subject_of = FROM } } }
			}
			ROOT = { transfer_state = PREV }
		}
		every_country = {
			limit = {
				is_subject_of = ROOT
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_owned_state = {
				limit = {
					is_core_of = ROOT
				}
				ROOT = { transfer_state = PREV }
			}
			ROOT = { end_puppet = PREV }
		}
		every_country = {
			limit = { OR = { tag = FROM is_in_faction_with = FROM } }
			white_peace = ROOT
		}
		ai_chance = {
			factor = 1
			modifier = { factor = 1.25 ROOT = { surrender_progress > 0.45 } }
			modifier = { factor = 1.25 ROOT = { surrender_progress > 0.40 } }
			modifier = { factor = 1.25 ROOT = { surrender_progress > 0.35 } }
			modifier = { factor = 1.25 ROOT = { surrender_progress > 0.30 } }
			modifier = { factor = 1.25 ROOT = { surrender_progress > 0.25 } }
			modifier = { factor = 0 ROOT = { surrender_progress < 0.2 } }
		}
	}
	option = {
		trigger = { ROOT = { NOT = { original_tag = POL } } }
		name = poland.13.b
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				is_subject_of = ROOT
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_state = {
				limit = {
					OR = { OWNER = { OR = { tag = FROM is_subject_of = FROM } } CONTROLLER = { OR = { tag = FROM is_subject_of = FROM } } }
					OR = {
						is_core_of = PREV
						is_claimed_by = PREV
					}
				}
				PREV = { transfer_state = PREV }
			}
		}
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = ROOT tag = FROM } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_state = {
			limit = {
				is_owned_by = FROM
				NOT = { is_core_of = FROM }
			}
			ROOT = { transfer_state = PREV }
		}
		every_state = {
			limit = {
				is_core_of = FROM
				OR = { OWNER = { OR = { tag = ROOT is_subject_of = ROOT } } CONTROLLER = { OR = { tag = ROOT is_subject_of = ROOT } } }
			}
			FROM = { transfer_state = PREV }
		}
		every_country = {
			limit = {
				is_subject_of = FROM
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_owned_state = {
				limit = {
					is_core_of = FROM
				}
				FROM = { transfer_state = PREV }
			}
			FROM = { end_puppet = PREV }
		}
		every_country = {
			limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
			FROM = { white_peace = PREV }
		}
		ai_chance = {
			factor = 2
			modifier = {
				factor = 0
				FROM = { surrender_progress > 0.5 }
			}
			modifier = { factor = 0.8 FROM = { surrender_progress > 0.45 } }
			modifier = { factor = 0.8 FROM = { surrender_progress > 0.40 } }
			modifier = { factor = 0.8 FROM = { surrender_progress > 0.35 } }
			modifier = { factor = 0.8 FROM = { surrender_progress > 0.30 } }
			modifier = { factor = 0.8 FROM = { surrender_progress > 0.25 } }
		}
	}
}

# Польша потерпела поражение от России
country_event = {
	id = poland.14
	title = poland.14.t
	desc = poland.14.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	trigger = {
		tag = RUS
		has_war_with = POL
		has_country_flag = RUS_Oktober_revolution_fired
		has_government = communism
		RUS = { is_subject = no }
		POL = { is_subject = no }
		NOT = { has_global_flag = RUS_civil_war_over }
		NOT = { has_global_flag = POL_RUS_peace_fired }
		POL = {
			surrender_progress > 0.8
			NOT = {
				controls_state = 10
			}
		}
	}
	
	fire_only_once = yes
	
	mean_time_to_happen = {
		days = 1
	}
	
	option = {
		name = poland.14.a
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				is_subject_of = RUS
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_state = {
				limit = {
					OWNER = {
						OR = {
							tag = POL
							is_subject_of = POL
						}
					}
					OR = {
						is_core_of = PREV
						is_claimed_by = PREV
					}
				}
				PREV = { transfer_state = PREV }
			}
		}
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = RUS tag = POL } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_state = {
			limit = {
				is_owned_by = POL
				NOT = { is_core_of = POL }
			}
			RUS = { transfer_state = PREV }
		}
		every_state = {
			limit = {
				is_owned_by = RUS
				is_core_of = POL
			}
			POL = { transfer_state = PREV }
		}
		every_country = {
			limit = { OR = { tag = RUS is_in_faction_with = RUS } }
			POL = { white_peace = PREV }
		}
		if = {
			limit = { RUS = { is_faction_leader = yes } }
			RUS = { add_to_faction = POL }
		}
		POL = {
			add_popularity = { ideology = communism popularity = 0.15 }
			set_politics = {
				ruling_party = communism
				elections_allowed = no
			}
			add_ideas = {
				military_dictatorship_gov_form
				local_assembly_decisions_b_law
				unitary_gov_terr
				allow_one_party_law
				ideological_expansion_law
				workers_control_law
				
				civilian_economy
				disarmed_nation
			}
			create_country_leader = { name = "Jozef Unszlicht" desc = "" picture = "gfx/leaders/SOV/Portrait_Jozef_Unszlicht.dds" ideology = leninism traits = { } }
		}
		RUS = { POL = { subjecting_script_with_transfer_parties_dominion = yes } }
	}
}

# Россия потерпела поражение от Польши
country_event = {
	id = poland.15
	title = poland.15.t
	desc = poland.15.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	trigger = {
		tag = RUS
		has_war_with = POL
		has_country_flag = RUS_Oktober_revolution_fired
		has_government = communism
		RUS = { is_subject = no }
		POL = { is_subject = no }
		NOT = { has_global_flag = RUS_civil_war_over }
		NOT = { has_global_flag = POL_RUS_peace_fired }
		RUS = {
			surrender_progress > 0.6
			NOT = {
				controls_state = 206
				controls_state = 202
				OR = {
					controls_state = 195
					controls_state = 219
					controls_state = 920
				}
			}
		}
	}
	
	fire_only_once = yes
	
	mean_time_to_happen = {
		days = 1
	}
	
	option = {
		name = poland.15.a
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				is_subject_of = POL
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_state = {
				limit = {
					OWNER = {
						OR = {
							tag = RUS
							is_subject_of = RUS
						}
					}
					OR = {
						is_core_of = PREV
						is_claimed_by = PREV
					}
				}
				PREV = { transfer_state = PREV }
			}
		}
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = RUS tag = POL } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_state = {
			limit = {
				is_owned_by = RUS
				OR = {
					is_core_of = POL
					is_claimed_by = POL
					is_core_of = BLR
					is_claimed_by = BLR
					is_core_of = UKR
					is_claimed_by = UKR
					is_core_of = LIT
					is_claimed_by = LIT
					is_core_of = LAT
					is_claimed_by = LAT
					is_core_of = EST
					is_claimed_by = EST
					is_core_of = CRI
					is_claimed_by = CRI
				}
			}
			POL = { transfer_state = PREV }
		}
		every_state = {
			limit = {
				is_owned_by = RUS
				OR = {
					state = 1351
					state = 809
					state = 210
					state = 242
					state = 243
					state = 1016
					#state = 1169
					state = 224
					state = 960
					state = 240
				}
			}
			POL = { transfer_state = PREV }
		}
		every_country = {
			limit = { OR = { tag = RUS is_in_faction_with = RUS } }
			POL = { white_peace = PREV }
		}
		if = {
			limit = { LIT = { has_war_with = RUS } }
			every_country = {
				limit = { OR = { tag = RUS is_in_faction_with = RUS } }
				LIT = { white_peace = PREV }
			}
			every_state = {
				limit = {
					is_owned_by = RUS
					is_core_of = LIT
				}
				LIT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { LAT = { has_war_with = RUS } }
			every_country = {
				limit = { OR = { tag = RUS is_in_faction_with = RUS } }
				LAT = { white_peace = PREV }
			}
			every_state = {
				limit = {
					is_owned_by = RUS
					is_core_of = LAT
				}
				LAT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { EST = { has_war_with = RUS } }
			every_country = {
				limit = { OR = { tag = RUS is_in_faction_with = RUS } }
				EST = { white_peace = PREV }
			}
			every_state = {
				limit = {
					is_owned_by = RUS
					is_core_of = EST
				}
				EST = { transfer_state = PREV }
			}
		}
	}
}

# Оповещалка о отклонении мирного предложения
country_event = {
	id = poland.17
	title = poland.17.t
	desc = poland.17.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		name = poland.17.a
		
	}
}

# Холмщина и Подляшье
country_event = {
	id = poland.18
	title = poland.18.t
	desc = poland.18.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	trigger = {
		tag = POL
		OR = {
			has_global_flag = WWI_GER_HAS_CAPITULATED
			GER = { has_country_flag = GER_monarchy_fall }
		}
		owns_state = 92
		any_country = {
			original_tag = UKR
			is_neighbor_of = POL
			owns_state = 904
			owns_state = 1165
		}
	}
	
	mean_time_to_happen = { days = 0 }
	
	fire_only_once = yes
	
	option = {
		ai_chance = {
			factor = 100
		}
		name = poland.18.a
		random_other_country = {
			limit = {
				original_tag = UKR
				owns_state = 904
				owns_state = 1165
			}
			country_event = { id = poland.19 hours = 2 }
		}
		transfer_state = 904
		transfer_state = 1165
	}
	option = {
		ai_chance = {
			factor = 0
		}
		name = poland.18.b
	}
}
# Холмщина и Подляшье
country_event = {
	id = poland.19
	title = poland.19.t
	desc = poland.19.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		ai_chance = {
			factor = 100
		}
		name = poland.19.a
	}
	option = {
		ai_chance = {
			factor = 0
		}
		name = poland.19.b
		FROM = {
			declare_war_on = {
				target = ROOT
				type = annex_everything
			}
		}
	}
}

# Оповещалка о переходе Украины в подданство Польши
country_event = {
	id = poland.21
	title = poland.21.t
	desc = poland.21.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		name = poland.21.a
		
		ai_chance = {
			factor = 1
		}
	}
	option = {
		name = poland.21.b
		
		every_state = {
			limit = {
				is_controlled_by = UKR
				OR = {
					is_core_of = UKR
					is_claimed_by = UKR
				}
				OWNER = {
					OR = {
						tag = ROOT
						is_in_faction_with = ROOT
						is_subject_of = ROOT
					}
				}
			}
			UKR = { transfer_state = PREV }
		}
		every_state = {
			limit = {
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_in_faction_with = ROOT
						is_subject_of = ROOT
					}
				}
				OR = {
					is_core_of = CONTROLLER
					is_claimed_by = CONTROLLER
					CONTROLLER = { OR = { former_russian_empire_ukraine_country = yes is_main_former_russian_empire_country = yes } }
				}
				is_owned_by = UKR
			}
			CONTROLLER = { transfer_state = PREV }
		}
		FROM = {
			OVERLORD = {
				ROOT = { white_peace = PREV }
			}
		}
		
		ai_chance = {
			factor = 4
			modifier = {
				factor = 0
				tag = RUS
			}
			modifier = {
				factor = 0
				former_russian_empire_ukraine_country = yes
			}
		}
	}
}