add_namespace = rus_poland_peace

# Польша или Россия предлагает другой стороне мир с большими уступками для Польши
country_event = {
	id = rus_poland_peace.11
	title = rus_poland_peace.11.t
	desc = rus_poland_peace.11.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		name = rus_poland_peace.11.a
		FROM = { country_event = rus_poland_peace.17 }
		ai_chance = {
			factor = 1
		}
	}
	option = {
		trigger = { ROOT = { original_tag = POL } }
		name = rus_poland_peace.11.b
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = FROM tag = ROOT } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_country = {
			limit = {
				is_subject_of = ROOT
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
				}
			}
			every_state = {
				limit = {
					OR = { OWNER = { OR = { tag = FROM is_subject_of = FROM } } CONTROLLER = { OR = { tag = FROM is_subject_of = FROM } } }
					OR = {
						is_core_of = PREV
						is_claimed_by = PREV
					}
				}
				PREV = { transfer_state = PREV }
			}
		}
		every_state = {
			limit = {
				OR = { OWNER = { OR = { tag = FROM is_subject_of = FROM } } CONTROLLER = { OR = { tag = FROM is_subject_of = FROM } } }
				OR = {
					is_core_of = ROOT
					is_claimed_by = ROOT
					is_core_of = BLR
					is_claimed_by = BLR
					is_core_of = LIT
					is_claimed_by = LIT
					is_core_of = COU
					is_claimed_by = COU
					is_core_of = UKR
					is_claimed_by = UKR
				}
			}
			ROOT = { transfer_state = PREV }
		}
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = FROM tag = ROOT } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		if = {
			limit = { LIT = { has_war_with = FROM } }
			every_country = {
				limit = { OR = { tag = FROM is_in_faction_with = FROM } }
				white_peace = LIT
				remove_wargoal = {
					type = all
					target = LIT
				}
				LIT = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = FROM
					is_core_of = LIT
				}
				LIT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { LAT = { has_war_with = FROM } }
			every_country = {
				limit = { OR = { tag = FROM is_in_faction_with = FROM } }
				white_peace = LAT
				remove_wargoal = {
					type = all
					target = LAT
				}
				LAT = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = FROM
					is_core_of = LAT
				}
				LAT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { EST = { has_war_with = FROM } }
			every_country = {
				limit = { OR = { tag = FROM is_in_faction_with = FROM } }
				white_peace = EST
				remove_wargoal = {
					type = all
					target = EST
				}
				EST = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = FROM
					is_core_of = EST
				}
				EST = { transfer_state = PREV }
			}
		}
		every_country = {
			limit = { OR = { tag = FROM is_in_faction_with = FROM } }
			ROOT = { THIS_white_peace_PREV_buffer = yes }
			remove_wargoal = {
				type = all
				target = ROOT
			}
			ROOT = {
				remove_wargoal = {
					type = all
					target = PREV
				}
			}
		}
		ai_chance = {
			factor = 2
		}
	}
	option = {
		trigger = { ROOT = { NOT = { original_tag = POL } } }
		name = rus_poland_peace.11.b
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = ROOT tag = FROM } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_country = {
			limit = {
				is_subject_of = FROM
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
				}
			}
			every_state = {
				limit = {
					OR = { OWNER = { OR = { tag = ROOT is_subject_of = ROOT } } CONTROLLER = { OR = { tag = ROOT is_subject_of = ROOT } } }
					OR = {
						is_core_of = PREV
						is_claimed_by = PREV
					}
				}
				PREV = { transfer_state = PREV }
			}
		}
		every_state = {
			limit = {
				OR = { OWNER = { OR = { tag = ROOT is_subject_of = ROOT } } CONTROLLER = { OR = { tag = ROOT is_subject_of = ROOT } } }
				OR = {
					is_core_of = FROM
					is_claimed_by = FROM
					is_core_of = BLR
					is_claimed_by = BLR
					is_core_of = LIT
					is_claimed_by = LIT
					is_core_of = COU
					is_claimed_by = COU
					is_core_of = UKR
					is_claimed_by = UKR
				}
			}
			FROM = { transfer_state = PREV }
		}
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = ROOT tag = FROM } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		if = {
			limit = { LIT = { has_war_with = ROOT } }
			every_country = {
				limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
				LIT = { THIS_white_peace_PREV_buffer = yes }
				remove_wargoal = {
					type = all
					target = LIT
				}
				LIT = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = ROOT
					is_core_of = LIT
				}
				LIT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { LAT = { has_war_with = ROOT } }
			every_country = {
				limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
				LAT = { THIS_white_peace_PREV_buffer = yes }
				remove_wargoal = {
					type = all
					target = LAT
				}
				LAT = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = ROOT
					is_core_of = LAT
				}
				LAT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { EST = { has_war_with = ROOT } }
			every_country = {
				limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
				EST = { THIS_white_peace_PREV_buffer = yes }
				remove_wargoal = {
					type = all
					target = EST
				}
				EST = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = ROOT
					is_core_of = EST
				}
				EST = { transfer_state = PREV }
			}
		}
		every_country = {
			limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
			FROM = { THIS_white_peace_PREV_buffer = yes }
			remove_wargoal = {
				type = all
				target = FROM
			}
			FROM = {
				remove_wargoal = {
					type = all
					target = PREV
				}
			}
		}
		ai_chance = {
			factor = 2
		}
	}
}

# Польша или Россия предлагает другой стороне историчный мир
country_event = {
	id = rus_poland_peace.12
	title = rus_poland_peace.12.t
	desc = rus_poland_peace.12.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		name = rus_poland_peace.12.a
		FROM = { country_event = rus_poland_peace.17 }
		ai_chance = {
			factor = 1
		}
	}
	option = {
		trigger = { ROOT = { original_tag = POL } }
		name = rus_poland_peace.12.b
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = FROM tag = ROOT } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_state = {
			limit = {
				is_owned_by = ROOT
				OR = {
					state = 1004
					region = 37
					region = 130
					region = 302
					region = 303
					state = 206
					state = 1401
					state = 924
					state = 1168
					state = 922
					state = 94
					state = 240
					region = 306
					region = 132
				}
				NOT = {
					is_core_of = ROOT
					is_claimed_by = ROOT
					is_core_of = LIT
					is_claimed_by = LIT
				}
			}
			FROM = { transfer_state = PREV }
		}
		every_state = {
			limit = {
				OR = {
					is_core_of = ROOT
					is_claimed_by = ROOT
					is_core_of = LIT
					is_claimed_by = LIT
				}
				OR = { OWNER = { OR = { tag = FROM is_subject_of = FROM } } CONTROLLER = { OR = { tag = FROM is_subject_of = FROM } } }
			}
			ROOT = { transfer_state = PREV }
		}
		every_country = {
			limit = {
				is_subject_of = ROOT
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_owned_state = {
				limit = {
					OR = {
						is_core_of = ROOT
						is_claimed_by = ROOT
						is_core_of = LIT
						is_claimed_by = LIT
					}
				}
				ROOT = { transfer_state = PREV }
			}
			ROOT = { end_puppet = PREV }
		}
		every_country = {
			limit = { OR = { tag = FROM is_in_faction_with = FROM } }
			ROOT = { THIS_white_peace_PREV_buffer = yes }
			remove_wargoal = {
				type = all
				target = ROOT
			}
			ROOT = {
				remove_wargoal = {
					type = all
					target = PREV
				}
			}
		}
		if = {
			limit = { LIT = { has_war_with = FROM } }
			every_country = {
				limit = { OR = { tag = FROM is_in_faction_with = FROM } }
				white_peace = LIT
				remove_wargoal = {
					type = all
					target = LIT
				}
				LIT = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = FROM
					is_core_of = LIT
				}
				LIT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { LAT = { has_war_with = FROM } }
			every_country = {
				limit = { OR = { tag = FROM is_in_faction_with = FROM } }
				white_peace = LAT
				remove_wargoal = {
					type = all
					target = LAT
				}
				LAT = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = FROM
					is_core_of = LAT
				}
				LAT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { EST = { has_war_with = FROM } }
			every_country = {
				limit = { OR = { tag = FROM is_in_faction_with = FROM } }
				white_peace = EST
				remove_wargoal = {
					type = all
					target = EST
				}
				EST = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = FROM
					is_core_of = EST
				}
				EST = { transfer_state = PREV }
			}
		}
		ai_chance = {
			factor = 4
		}
	}
	option = {
		trigger = { ROOT = { NOT = { original_tag = POL } } }
		name = rus_poland_peace.12.b
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = ROOT tag = FROM } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_state = {
			limit = {
				is_owned_by = FROM
				OR = {
					state = 1004
					region = 37
					region = 130
					region = 302
					region = 303
					state = 206
					state = 1401
					state = 924
					state = 1168
					state = 922
					state = 94
					state = 240
					region = 306
					region = 132
				}
				NOT = {
					is_core_of = FROM
					is_claimed_by = FROM
					is_core_of = LIT
					is_claimed_by = LIT
				}
			}
			ROOT = { transfer_state = PREV }
		}
		every_state = {
			limit = {
				OR = {
					is_core_of = FROM
					is_claimed_by = FROM
					is_core_of = LIT
					is_claimed_by = LIT
				}
				OR = { OWNER = { OR = { tag = ROOT is_subject_of = ROOT } } CONTROLLER = { OR = { tag = ROOT is_subject_of = ROOT } } }
			}
			FROM = { transfer_state = PREV }
		}
		every_country = {
			limit = {
				is_subject_of = FROM
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_owned_state = {
				limit = {
					OR = {
						is_core_of = FROM
						is_claimed_by = FROM
						is_core_of = LIT
						is_claimed_by = LIT
					}
				}
				FROM = { transfer_state = PREV }
			}
			FROM = { end_puppet = PREV }
		}
		every_country = {
			limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
			FROM = { THIS_white_peace_PREV_buffer = yes }
			remove_wargoal = {
				type = all
				target = ROOT
			}
			ROOT = {
				remove_wargoal = {
					type = all
					target = PREV
				}
			}
		}
		if = {
			limit = { LIT = { has_war_with = ROOT } }
			every_country = {
				limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
				LIT = { THIS_white_peace_PREV_buffer = yes }
				remove_wargoal = {
					type = all
					target = LIT
				}
				LIT = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = ROOT
					is_core_of = LIT
				}
				LIT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { LAT = { has_war_with = ROOT } }
			every_country = {
				limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
				LAT = { THIS_white_peace_PREV_buffer = yes }
				remove_wargoal = {
					type = all
					target = LAT
				}
				LAT = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = ROOT
					is_core_of = LAT
				}
				LAT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { EST = { has_war_with = ROOT } }
			every_country = {
				limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
				EST = { THIS_white_peace_PREV_buffer = yes }
				remove_wargoal = {
					type = all
					target = EST
				}
				EST = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = ROOT
					is_core_of = EST
				}
				EST = { transfer_state = PREV }
			}
		}
		ai_chance = {
			factor = 4
		}
	}
}

# Польша или Россия предлагает другой стороне мир (Польша замкнута в границах царства польского)
country_event = {
	id = rus_poland_peace.13
	title = rus_poland_peace.13.t
	desc = rus_poland_peace.13.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		name = rus_poland_peace.13.a #Отказ
		FROM = { country_event = rus_poland_peace.17 }
		ai_chance = {
			factor = 2
		}
	}
	option = {
		trigger = { ROOT = { original_tag = POL } }
		name = rus_poland_peace.13.b
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				is_subject_of = FROM
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_state = {
				limit = {
					OR = { OWNER = { OR = { tag = ROOT is_subject_of = ROOT } } CONTROLLER = { OR = { tag = ROOT is_subject_of = ROOT } } }
					OR = {
						is_core_of = PREV
						is_claimed_by = PREV
					}
				}
				PREV = { transfer_state = PREV }
			}
		}
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = FROM tag = ROOT } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = ROOT is_subject_of = ROOT tag = FROM is_subject_of = FROM } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_state = {
			limit = {
				is_owned_by = ROOT
				NOT = { is_core_of = ROOT }
			}
			FROM = { transfer_state = PREV }
		}
		every_state = {
			limit = {
				is_core_of = ROOT
				OR = { OWNER = { OR = { tag = FROM is_subject_of = FROM } } CONTROLLER = { OR = { tag = FROM is_subject_of = FROM } } }
			}
			ROOT = { transfer_state = PREV }
		}
		every_country = {
			limit = {
				is_subject_of = ROOT
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_owned_state = {
				limit = {
					is_core_of = ROOT
				}
				ROOT = { transfer_state = PREV }
			}
			ROOT = { end_puppet = PREV }
		}
		every_country = {
			limit = { OR = { tag = FROM is_in_faction_with = FROM } }
			ROOT = { THIS_white_peace_PREV_buffer = yes }
			remove_wargoal = {
				type = all
				target = ROOT
			}
			ROOT = {
				remove_wargoal = {
					type = all
					target = PREV
				}
			}
		}
		ai_chance = {
			factor = 1
			modifier = { factor = 1.25 ROOT = { surrender_progress > 0.45 } }
			modifier = { factor = 1.25 ROOT = { surrender_progress > 0.40 } }
			modifier = { factor = 1.25 ROOT = { surrender_progress > 0.35 } }
			modifier = { factor = 1.25 ROOT = { surrender_progress > 0.30 } }
			modifier = { factor = 1.25 ROOT = { surrender_progress > 0.25 } }
			modifier = { factor = 0 ROOT = { surrender_progress < 0.2 } }
		}
	}
	option = {
		trigger = { ROOT = { NOT = { original_tag = POL } } }
		name = rus_poland_peace.13.b
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				is_subject_of = ROOT
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_state = {
				limit = {
					OR = { OWNER = { OR = { tag = FROM is_subject_of = FROM } } CONTROLLER = { OR = { tag = FROM is_subject_of = FROM } } }
					OR = {
						is_core_of = PREV
						is_claimed_by = PREV
					}
				}
				PREV = { transfer_state = PREV }
			}
		}
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = ROOT tag = FROM } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = FROM is_subject_of = FROM tag = ROOT is_subject_of = ROOT } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_state = {
			limit = {
				is_owned_by = FROM
				NOT = { is_core_of = FROM }
			}
			ROOT = { transfer_state = PREV }
		}
		every_state = {
			limit = {
				is_core_of = FROM
				OR = { OWNER = { OR = { tag = ROOT is_subject_of = ROOT } } CONTROLLER = { OR = { tag = ROOT is_subject_of = ROOT } } }
			}
			FROM = { transfer_state = PREV }
		}
		every_country = {
			limit = {
				is_subject_of = FROM
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_owned_state = {
				limit = {
					is_core_of = FROM
				}
				FROM = { transfer_state = PREV }
			}
			FROM = { end_puppet = PREV }
		}
		every_country = {
			limit = { OR = { tag = ROOT is_in_faction_with = ROOT } }
			FROM = { THIS_white_peace_PREV_buffer = yes }
			remove_wargoal = {
				type = all
				target = FROM
			}
			FROM = {
				remove_wargoal = {
					type = all
					target = PREV
				}
			}
		}
		ai_chance = {
			factor = 2
			modifier = {
				factor = 0
				FROM = { surrender_progress > 0.5 }
			}
			modifier = { factor = 0.8 FROM = { surrender_progress > 0.45 } }
			modifier = { factor = 0.8 FROM = { surrender_progress > 0.40 } }
			modifier = { factor = 0.8 FROM = { surrender_progress > 0.35 } }
			modifier = { factor = 0.8 FROM = { surrender_progress > 0.30 } }
			modifier = { factor = 0.8 FROM = { surrender_progress > 0.25 } }
		}
	}
}

# Польша потерпела поражение от России
country_event = {
	id = rus_poland_peace.14
	title = rus_poland_peace.14.t
	desc = rus_poland_peace.14.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	trigger = {
		tag = RUS
		has_war_with = POL
		has_country_flag = RUS_Oktober_revolution_fired
		has_government = communism
		RUS = { is_subject = no }
		POL = { is_subject = no }
		NOT = { has_global_flag = RUS_civil_war_over }
		NOT = { has_global_flag = POL_RUS_peace_fired }
		POL = {
			surrender_progress > 0.8
			NOT = {
				controls_state = 10
			}
		}
	}
	
	fire_only_once = yes
	
	mean_time_to_happen = {
		days = 1
	}
	
	option = {
		name = rus_poland_peace.14.a
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				is_subject_of = RUS
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_state = {
				limit = {
					OWNER = {
						OR = {
							tag = POL
							is_subject_of = POL
						}
					}
					OR = {
						is_core_of = PREV
						is_claimed_by = PREV
					}
				}
				PREV = { transfer_state = PREV }
			}
		}
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = RUS tag = POL } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_state = {
			limit = {
				is_owned_by = POL
				NOT = { is_core_of = POL }
			}
			RUS = { transfer_state = PREV }
		}
		every_state = {
			limit = {
				is_owned_by = RUS
				is_core_of = POL
			}
			POL = { transfer_state = PREV }
		}
		every_country = {
			limit = { OR = { tag = RUS is_in_faction_with = RUS } }
			POL = { THIS_white_peace_PREV_buffer = yes }
			remove_wargoal = {
				type = all
				target = POL
			}
			POL = {
				remove_wargoal = {
					type = all
					target = PREV
				}
			}
		}
		if = {
			limit = { RUS = { is_faction_leader = yes } }
			RUS = { add_to_faction = POL }
		}
		POL = {
			add_popularity = { ideology = communism popularity = 0.15 }
			set_politics = {
				ruling_party = communism
				elections_allowed = no
			}
			add_ideas = {
				military_dictatorship_gov_form
				local_assembly_decisions_b_law
				unitary_gov_terr
				allow_one_party_law
				ideological_expansion_law
				workers_control_law
				
				civilian_economy
				disarmed_nation
			}
			create_country_leader = { name = "Jozef Unszlicht" desc = "" picture = "gfx/leaders/SOV/Portrait_Jozef_Unszlicht.dds" ideology = leninism traits = { } }
		}
		every_country = {
			limit = { is_subject_of = POL }
			POL = { end_puppet = PREV }
		}
		RUS = { POL = { subjecting_script_with_transfer_parties_military_political_alliance = yes } }
	}
}

# Россия потерпела поражение от Польши
country_event = {
	id = rus_poland_peace.15
	title = rus_poland_peace.15.t
	desc = rus_poland_peace.15.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	trigger = {
		tag = RUS
		has_war_with = POL
		has_country_flag = RUS_Oktober_revolution_fired
		has_government = communism
		RUS = { is_subject = no }
		POL = { is_subject = no }
		NOT = { has_global_flag = RUS_civil_war_over }
		NOT = { has_global_flag = POL_RUS_peace_fired }
		RUS = {
			surrender_progress > 0.6
			NOT = {
				controls_state = 206
				controls_state = 202
				OR = {
					controls_state = 195
					controls_state = 219
					controls_state = 920
				}
			}
		}
	}
	
	fire_only_once = yes
	
	mean_time_to_happen = {
		days = 1
	}
	
	option = {
		name = rus_poland_peace.15.a
		set_global_flag = POL_RUS_peace_fired
		every_country = {
			limit = {
				is_subject_of = POL
				OR = {
					original_tag = BLR
					original_tag = UKR
					original_tag = COU
					original_tag = LIT
					original_tag = LAT
					original_tag = EST
					original_tag = CRI
				}
			}
			every_state = {
				limit = {
					OWNER = {
						OR = {
							tag = RUS
							is_subject_of = RUS
						}
					}
					OR = {
						is_core_of = PREV
						is_claimed_by = PREV
					}
				}
				PREV = { transfer_state = PREV }
			}
		}
		every_country = {
			limit = {
				OVERLORD = { OR = { tag = RUS tag = POL } }
				OR = {
					AND = {
						original_tag = BLR
						NOT = {
							206 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						original_tag = LIT
						NOT = {
							11 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						original_tag = LAT
						NOT = {
							12 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						original_tag = EST
						NOT = {
							812 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
					AND = {
						former_russian_empire_ukraine_country = yes
						NOT = {
							202 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							221 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							192 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							227 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
							196 = { OR = { OWNER = { tag = PREV.PREV } CONTROLLER = { has_war_with = PREV.PREV NOT = { tag = POL is_subject_of = POL tag = RUS is_subject_of = RUS } } } }
						}
					}
				}
			}
			OVERLORD = {
				annex_country = {
					target = PREV
					transfer_troops = yes
				}
			}
		}
		every_state = {
			limit = {
				is_owned_by = RUS
				OR = {
					is_core_of = POL
					is_claimed_by = POL
					is_core_of = BLR
					is_claimed_by = BLR
					is_core_of = UKR
					is_claimed_by = UKR
					is_core_of = LIT
					is_claimed_by = LIT
					is_core_of = LAT
					is_claimed_by = LAT
					is_core_of = EST
					is_claimed_by = EST
					is_core_of = CRI
					is_claimed_by = CRI
				}
			}
			POL = { transfer_state = PREV }
		}
		every_state = {
			limit = {
				is_owned_by = RUS
				OR = {
					state = 1351
					state = 809
					state = 210
					state = 242
					state = 243
					state = 1016
					#state = 1169
					state = 224
					state = 960
					state = 240
				}
			}
			POL = { transfer_state = PREV }
		}
		every_country = {
			limit = { OR = { tag = RUS is_in_faction_with = RUS } }
			POL = { THIS_white_peace_PREV_buffer = yes }
			remove_wargoal = {
				type = all
				target = POL
			}
			POL = {
				remove_wargoal = {
					type = all
					target = PREV
				}
			}
		}
		if = {
			limit = { LIT = { has_war_with = RUS } }
			every_country = {
				limit = { OR = { tag = RUS is_in_faction_with = RUS } }
				LIT = { THIS_white_peace_PREV_buffer = yes }
				remove_wargoal = {
					type = all
					target = LIT
				}
				LIT = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = RUS
					is_core_of = LIT
				}
				LIT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { LAT = { has_war_with = RUS } }
			every_country = {
				limit = { OR = { tag = RUS is_in_faction_with = RUS } }
				LAT = { THIS_white_peace_PREV_buffer = yes }
				remove_wargoal = {
					type = all
					target = LAT
				}
				LAT = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = RUS
					is_core_of = LAT
				}
				LAT = { transfer_state = PREV }
			}
		}
		if = {
			limit = { EST = { has_war_with = RUS } }
			every_country = {
				limit = { OR = { tag = RUS is_in_faction_with = RUS } }
				EST = { THIS_white_peace_PREV_buffer = yes }
				remove_wargoal = {
					type = all
					target = EST
				}
				EST = {
					remove_wargoal = {
						type = all
						target = PREV
					}
				}
			}
			every_state = {
				limit = {
					is_owned_by = RUS
					is_core_of = EST
				}
				EST = { transfer_state = PREV }
			}
		}
	}
}

# Оповещалка о отклонении мирного предложения
country_event = {
	id = rus_poland_peace.17
	title = rus_poland_peace.17.t
	desc = rus_poland_peace.17.desc

	picture = GFX_report_event_greece_civil_war_soldiers
	
	is_triggered_only = yes
	
	option = {
		name = rus_poland_peace.17.a
		
	}
}